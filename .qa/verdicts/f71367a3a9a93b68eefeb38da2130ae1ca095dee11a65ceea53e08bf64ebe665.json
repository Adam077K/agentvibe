{
  "subject": "f71367a3a9a93b68eefeb38da2130ae1ca095dee11a65ceea53e08bf64ebe665",
  "base": "7f7bddd75b1eced00a2fc1fd1b2594ab5061376b",
  "tier": "full",
  "tier_driver": "scripts/pre-tool-use.test.mjs",
  "verdict": "PASS",
  "by": "builder-case-spelling-vacuity",
  "run_id": null,
  "evidence": "AUTHOR-RECORDED ON EXECUTION EVIDENCE, NOT AN INDEPENDENT REVIEW — stated so the weight is a choice. Single model family; no non-Anthropic judge exists in this runtime. Test-only diff: .claude/hooks/pre-tool-use.sh is byte-for-byte unchanged (git diff 7f7bddd..HEAD -- .claude/ is empty), and it was reverted-and-restored twice during verification with the restore proved by git. WHAT WAS WRONG: four assertions inside scripts/pre-tool-use.test.mjs exited with a bare 'return' conditional on an environment probe. A test that deletes itself where its premise is absent is indistinguishable from one that passed. THREE CONSTRUCTS, THREE DIFFERENT RIGHT ANSWERS, EACH DECIDED BY A MEASUREMENT: (1) THE CASE-SPELLING CASE -> (c) assert the universal property. The premise (a case-folding filesystem) is absent on every Linux runner, the inverse polarity of the gh case and of the $HOME fixture, so none of those decisions was inherited. The hook's own claim — containment is decided the way the FILESYSTEM decides it — holds on BOTH kinds of filesystem, so the expectation is now DERIVED from device+inode identity rather than assumed from the OS: same directory -> ALLOW, different directory -> BLOCK and fail closed. Neither branch is a skip. fs.existsSync was the wrong probe as well as the wrong polarity: a case-sensitive volume holding foo and Foo answers yes about two genuinely different directories, and an expectation derived from that answer is the wrong one. (a) was rejected with the measurement: the half the RUNNER lacks is case-INsensitivity, which on Linux needs tune2fs -O casefold on an unmounted filesystem or a loopback vfat mount — root and a loop device inside a security test that also runs under a sandbox where sudo -n true returns 'operation not permitted'. THE GAIN IS REAL AND BOUNDED, AND I ONLY KNOW THAT BECAUSE I PUT THE BUG BACK: with -ef reverted to a string comparison the ALLOW branch goes RED (2 !== 0), and the BLOCK branch answers 2 either way, because pwd -P already normalises symlinks on both sides and case is the ONLY axis the two operators disagree on. So the runner went from asserting NOTHING to asserting fail-closed, not from nothing to covering case; t.diagnostic prints which branch ran so a green CI log cannot be read as the stronger claim. (2) and (3) THE TWO KNOWN-BUG INHERITED-ROOT PINS -> (a) construct the condition. Both keyed on path.resolve(REPO,'..','ceo-1-1787176362'), one long-gone session's directory, absent on every machine including the one that wrote them, so both returned silently and had been documenting nothing. Constructible because they never ran git against the sibling, only used it as a path. Now built with premises asserted, and BOTH PINNED BEHAVIOURS REPRODUCE UNCHANGED — BLOCK(2) and ALLOW(0) — so no expectation moved and no security policy was decided here. FIXTURE LOCATION MEASURED, NOT HABITUAL: os.tmpdir() realpaths to /private/tmp/claude-501, byte-for-byte the scratchpad root the hook allows outright, so a temp-dir fixture would have made the ALLOW pin pass for the wrong reason; $HOME and /tmp are both EPERM under the armed sandbox; under REPO is writable in the session root and in a nested worktree. Proof: pointing the fixture back at os.tmpdir() fires the premise assertion by name. (4) THE SCRATCHPAD CASE -> (b) skip, loudly, and the polarity is the reason it differs from the $HOME precedent one screen below it: THERE CI can build the fixture so a runner skip would hide a real failure; HERE CI cannot — /private/tmp/claude-<uid> needs root on Linux — so failing would make main permanently red for an environmental fact. The reason string names the absent premise and the command that ends it, and the exemption's NARROWNESS, which is the dangerous direction, still runs on every machine. MY OWN FIRST CONTROL WAS THE DEFECT IT WAS WRITTEN TO PREVENT: a detector forced to 'return true' passed all 168, because the only negative control compared REPO with a nonexistent path where fs.statSync throws and the catch returns false — it tested the try/catch and never reached the dev/ino comparison. Replaced with two directories that both exist; the mutant now fires by name. NON-VACUITY, FOUR MUTANTS, EACH FIRING ON THE INTENDED CONTROL WITH THE CAUSE IN THE MESSAGE: hook -ef -> '=' reddens the case test (2 !== 0); detector stuck true -> 167 pass 1 fail on 'never reached the dev/ino comparison'; detector stuck false -> 166 pass 2 fail on 'an unresolvable root has to fail CLOSED'; fixture base -> os.tmpdir() -> 166 pass 2 fail naming the scratchpad root. SWEEP, MECHANICAL: 13 candidate early-exits/skips across 28 test files in scripts/. 4 fixed here, 1 false positive (the word 'return' in a comment), 7 already-stated skips with reason strings (5 HOME_SKIP, 2 NEEDS_OBJECTS), 1 reported not fixed — run-gate.test.mjs:297, degraded but NOT vacuous because proof 1 asserts before it. FLOOR: test:pre-tool-use 168 pass 0 fail 0 skipped (was 167 with four silent returns inside them); npm run check Tally 43 of 43 passed 0 failed 141.7s. RESIDUAL STATED: on a case-sensitive filesystem the case-folding regression remains unobservable — that is a property of the filesystem, not of this test, and no change to this file can cure it. Full record: docs/08-agents_work/sessions/2026-08-26-builder-case-spelling-vacuity.md",
  "recorded_at": "2026-08-26T08:28:31.557Z"
}
