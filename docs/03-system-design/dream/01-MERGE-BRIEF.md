# Merge the dreams into one system · 2026-09-02

Three minds dreamed the same system from three seeds without seeing each other: **WAKE**
(`2026-09-02-dream-1.md`), **VIGIL** (`2026-09-02-dream-2.md`), **PLY** (`2026-09-02-dream-3.md`). Read
`00-BRIEF.md` first — the founder's ask is the whole brief — then the three dreams in full.

**Your job: one system, not a blend.** Where they agree — and they agree on the spine — take it and say so.
Where they differ, DECIDE, say why, and keep the losing image by name. A decision is one image. Provenance on
every paragraph: `(WAKE)` `(VIGIL)` `(PLY)` or `NEW`, and `NEW` justified in one line. Nothing is bound by the
existing harness or its spec; the parts bin (`docs/03-system-design/STARTUP-OS.md`, `docs/02-competitive/expansion/`)
may be opened last, to name which existing parts survive as organs of this system and which do not.

**What the merged document must contain, in any form you choose.** A name. The one sentence. The inversion it
rests on. Its organs and its laws. The unit of work. How autonomy is computed (the three dreams each gave a
formula — instruments, calibration of a founder model, reversal cost and track record — decide whether it is
one function or three, and write it). Memory and its metabolism. How it learns a field it has never seen. How
it finds and compiles context. The night, hour by hour. The day, and the founder's life. The reserved capacity
for being wrong, and why it cannot be spent down. How it checks itself without gates. The one line it never
crosses. What it costs. The first week with a founder who has only an idea. The year. What it would take to
exist, in three buckets: buildable today · needs the trend to hold · genuinely unsolved. Why nobody has seen
it. Then: where the three dreams disagreed and what you decided. Then, last: which parts of the existing harness
survive as organs, renamed if needed, and which are retired, in one table.

Research only. Nothing leaves the machine. Write `docs/03-system-design/dream/2026-09-02-THE-SYSTEM.md`.
Return ≤ 200 words: the name, the one sentence, the three decisions that most shaped it, and what you left out.
