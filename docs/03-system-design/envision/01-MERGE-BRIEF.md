# Merge round two into one system · 2026-09-02

Three minds envisioned the whole system without seeing each other: **mind-1**, **mind-2**, **mind-3**
(`2026-09-02-mind-{1,2,3}.md`). Each wrote a spine before reading anything, then learned from the prior
systems, then dreamed the whole. Read `00-BRIEF.md` first; the founder's words are the whole brief. Then the
three minds in full. Then **WAKE**, `docs/03-system-design/dream/2026-09-02-THE-SYSTEM.md`, round one's answer,
which the minds read as context and which you read as the strongest prior; and its three dreams beside it if
you need to trace an idea to its origin.

**Your job: one system, not a blend.** Where the minds agree, and especially where their spines agreed before
any of them had read a word, take it and say so: blind agreement is the strongest evidence this method
produces. Where they differ, DECIDE, say why, and keep the losing image by name. A decision is one image.
Provenance on every paragraph: `(M1)` `(M2)` `(M3)` `(WAKE)` or `(NEW: one line saying why)`. Where a mind
took an idea from WAKE, the tag names both.

**What the merged document must contain, in any form you choose.** A name. The one sentence. The inversion it
rests on. Its organs and its laws. The unit of work. How autonomy is computed. Memory and its metabolism. How it
learns a field it has never seen. How it finds and compiles context. **The drive: how it decides what to do
next and when to do nothing, per kind of project, who opens a goal, and how it never burns tokens without
direction. The founder's environment: terminal, phone, Mac, seeing the agents work, giving tasks, seeing
progress; the picture that is right for this founder, and the tension with a one-page stance if there is one.
The fleet: many projects, one founder, one budget, what is shared and never shared. The door: how a repository,
a skill or a tool from the world enters, and which of the catalogued 177 come in first. Storage: where every
store lives, in what form, at what size, what survives a dead machine. The field map: every job a company
needs, each with its ground truth and its class of judge, and the security fields from day one; and the
answer to the founder's question, harness or organisation.** The night. The day, and the founder's life. The
reserved capacity for being wrong. How it checks itself without gates. The one line it never crosses. What it
costs. The first week with a founder who has only an idea. The year. What it would take to exist, in three
buckets: buildable today · needs the trend to hold · genuinely unsolved. Why nobody has seen it. Then: where
the three minds disagreed and what you decided, one row per decision, losing images kept by name. Then: **what
this round dropped from WAKE, and whether it should have**, one row per organ or law of WAKE that no mind
kept, with your decision. Then, last: which parts of the existing harness survive as organs, renamed if needed,
and which are retired, in one table; WAKE's §22 is the prior for this and you may take its rows where nothing
changed them.

Research only. Nothing leaves the machine. WAKE stays on disk untouched as the record of round one. Write
`docs/03-system-design/envision/2026-09-02-THE-SYSTEM.md`. Return ≤ 200 words: the name, the one sentence, the
three decisions that most shaped it, what you kept from WAKE against the minds and what you dropped, and what
you left out.
