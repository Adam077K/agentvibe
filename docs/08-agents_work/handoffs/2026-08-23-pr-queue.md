# The PR queue — ready to paste

> **SUPERSEDED 2026-08-23. Do not run anything in this file.** No PR was ever opened from it. The nine
> branches were merged directly into `main` in the order given in
> [2026-08-23-after-p0.md](2026-08-23-after-p0.md) §1, `main` moved `5b8e127` → `413a029`, and every
> branch named below has been deleted from the remote — so each `gh pr create --head …` here now names a
> ref that does not exist. It is kept because the **bodies** are an accurate record of what each branch
> did and why, written before the outcome was known.

**From:** ceo (`ceo-4-1787176363`) · **Date:** 2026-08-23 · **Base:** `main` = `5b8e127`

> I cannot open these. `gh` reads `~/.config/gh/config.yml`, which the armed sandbox's `denyRead`
> covers, so every `gh` invocation dies before it parses a flag. `git push` is unaffected — it
> authenticates through osxkeychain, which a filesystem deny cannot reach. Every branch below is
> **pushed and verified on GitHub**; only the PR creation is left, and it is yours.

Ahead-counts and tiers were re-derived on 2026-08-23. They go stale the moment anything is appended,
so re-derive rather than quote.

---

## Merge order matters — read this before opening anything

`feat/gate-pr-route` is **stacked on** `feat/gate-and-provenance-v2` and must merge after it. Everything
else is independent. Two files are touched by more than one branch:

- **`package.json`** — the `check` chain. Resolve by **union**, never "take mine": a naive resolution
  silently drops a test from CI, which is the exact un-gating the merge-gate work exists to prevent.
- **`.claude/memory/CODEBASE-MAP.md`** — generated. Take **either** side wholesale, then
  `npm run build:map`. `check:map` fails if you get it wrong, so it is self-checking. **Never hand-merge.**

**`feat/gate-and-provenance` no longer exists on the remote.** It is now
`archive/gate-and-provenance-superseded` at `515f5e7` — renamed, not deleted, because it holds **15
commits that are not in v2** (the two lineages are disjoint; `merge-base --is-ancestor` returns false).
The copy was verified SHA-for-SHA before the old name was removed. Do not merge it: it ships the four
defects listed under PR 1.

## The tier label is not optional

`qa-lead-pass.yml` reads it. Derive it, do not guess:

```
git diff --name-only main..<branch> | xargs node scripts/classify.mjs
```

**Pass the paths as real arguments.** In zsh an unquoted `$list` is a single argument, and the
classifier then answers for the wrong path — it told me `fix/audit-repairs` was `lite` when it is
`irreversible`. Caught only because a branch touching `.claude/hooks/` cannot be `lite`. Re-run through
`xargs` and it is right.

---

## 1 · `feat/gate-and-provenance-v2` — 26 commits · **irreversible** · reviewed PASS

```
gh pr create --head feat/gate-and-provenance-v2 --base main --label risk:irreversible \
  --title "feat(gate,provenance): P0.5 plus a merge gate bound to the diff it reviewed"
```

Body:

> Lens provenance validates against a vendored content-addressed manifest, so a generated project passes
> the lint instead of failing before anyone touches it. And `warroom merge` now refuses a merge with no
> verdict bound to the diff that was reviewed.
>
> The anchor is `sha256` of the diff against `merge-base origin/main`, excluding the verdicts path so
> that recording a verdict does not move its own subject. Two properties are pinned as tests because
> together they are the whole claim: **the subject survives committing the verdict** — the property
> PR #77 got wrong, since a HEAD-SHA key stops existing the moment the record is committed — and **the
> subject dies to a one-byte edit** of the reviewed diff. 21 tests drive `bin/warroom merge` against
> throwaway repositories.
>
> Four defects fixed relative to the first attempt, now `archive/gate-and-provenance-superseded`:
> - **Tier 3 refuses instead of resolving.** It used to pipe a conflicted file to a model, write the raw
>   output back, commit it and delete the branch. Reproduction showed it discarding the branch's
>   reviewed line. What landed was never what anyone approved.
> - **No fallback to `$PROJECT_DIR/scripts/verdict.mjs`.** The merged repository does not get to supply
>   its own gate. Refusing to find a checker is safe; finding the wrong one is not.
> - **No `continue-on-error` anywhere in the file.** A live claim asserts its absence.
> - **The subject pathspec uses `:(exclude,glob)`.** Without `glob` a default git wildcard matches `/`,
>   so a nested `.qa/verdicts/**` file stayed invisible to the hash.
>
> Also: `branch -D` became `branch -d` at three merge exits, and the fake `tier=fast-forward` string is
> split into real tier and strategy fields.
>
> Reviewed PASS across three rounds (FAIL → PASS → PASS). Single model family — see the standing
> limitation at the end of this queue.

## 2 · `fix/audit-repairs` — 15 commits · **irreversible** · reviewed PASS · closes #95, #96

```
gh pr create --head fix/audit-repairs --base main --label risk:irreversible \
  --title "fix(audit): the org chart is true, the gate ref is cwd-independent, the hook stops crying wolf"
```

Body:

> Closes #95. Closes #96.
>
> Three holes an audit round found and never landed:
> - **The org chart was false.** `CLAUDE.md` and `AGENTS.md` described a 20-agent organisation; the
>   roster is **7 engines across 18 files**. `check-registration.mjs` gains check 12 — an agent name in
>   prose must resolve to a real file — so the document cannot drift back.
> - **`check-dispatch-agenttype` now covers `.md` workflows**, closing three phantom agentTypes in
>   `design-screen.md` that were falling back to `general-purpose` with tools `*`.
> - **#95** the gate ref was cwd-dependent; **#96** hook false positives.
>
> **The declared tier understates the floor.** The session files say `tier: full`; the classifier says
> **irreversible**, via `.claude/hooks/pre-tool-use.sh`. The gate accepts `full|irreversible`, so this
> passes as written — but the label above is the derived floor, not the declared one.
>
> **Known and inherited, not introduced:** `scripts/check-dispatch-agenttype.mjs:267-268` takes an
> unsanitised `agentType` into a template literal and then into `path.join(ROOT, rel)`. Reproduced: with
> the target present the checker **silently accepts** an `agentType` resolving outside `.claude/agents/`,
> which defeats the check's own purpose more than the file read does. `agentInfo` is byte-identical on
> `main`, so this branch inherits it. Filed rather than fixed here, to keep an already-irreversible diff
> reviewable.
>
> Reviewed PASS. Single model family.

## 3 · `chore/memory-eviction` — 11 commits · **full** · reviewed PASS

```
gh pr create --head chore/memory-eviction --base main --label risk:full \
  --title "chore(memory): rotate DECISIONS.md by type key, and cap the archive it rotates into"
```

Body:

> `DECISIONS.md` had **91 bytes of headroom** — 39,909 against a 40,000-byte cap — while `CLAUDE.md`
> told every agent it had "≤50 entries (archive when full)" of room. The entry cap sat at 23/50 and
> would never bind. Any breadcrumb longer than 91 bytes turned a blocking CI step red, and nothing said
> so.
>
> Entries rotate into `DECISIONS_ARCHIVE.md` by the **type keys already on every entry**
> (`Reversibility:`, `Affects:`), never by recency. Archival leaves a residue — a stub with
> `superseded_by:` or a surviving `kind: pattern` — because archival without residue is deletion with
> extra steps. The archive is **capped too**: it was 18,538 bytes checked by nothing, so otherwise the
> pressure just relocates. `CLAUDE.md`'s sentence is corrected in the same PR as the mechanism.
>
> **The ceiling is real and documented rather than hidden.** Two honesty commits consumed 3,440 B — 36%
> of what the eviction freed — because stubs asserting "no citations" were false, and a false negative
> in permanent memory is a scheduled deletion of a still-referenced record. A fourth round then came in
> 621 B *smaller* and more accurate, so the series is three up then one down, not a one-way ratchet.
> **Do not start a fifth hand-written round**: a stub obliged to enumerate every citation converges on
> the size of the entry it evicts. The two honest exits are a hard byte ceiling on the stub format, or
> citations recorded outside `DECISIONS.md` — and since nothing currently links a claim to a DECISIONS
> entry, the second is the same work as building that link. Do them as one thing.
>
> Reviewed PASS across four rounds (FAIL → PASS → FAIL → PASS). Single model family.

---

## Standing limitation, on every irreversible PR in this queue

Irreversible tier asks for **2-of-3 multi-judge**, and the `risk: high` predicate requires **≥2 distinct
model families**. There is no non-Anthropic model inside Claude Code, so every review backing this queue
was single-family and says so in its session file. This is a founder decision, not a task: accept
single-family review for harness self-edits, or land the Codex resolver first and gate on it. It was
raised unprompted on all eight review rounds of the previous session, and no amount of further work
inside this runtime resolves it.
