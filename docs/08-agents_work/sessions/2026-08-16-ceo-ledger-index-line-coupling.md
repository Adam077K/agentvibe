---
date: 2026-08-16
role: ceo
task: ledger-index-line-coupling
tier: full
qa_verdict: PASS
---

PR #52 — `.claude/ledger/index.json` stored `source_line` per claim, so a prose edit above a claim failed `ledger build --check` although no claim changed, and the error's evidence was `on disk: 19749 bytes · regenerated: 19749 bytes`. The byte count was not merely non-discriminating here: a one-line shift rewrites `295` as `296`, the same width, so it was **guaranteed equal for the entire class of failure it existed to explain**. Field dropped, `locate` added to resolve positions live, message rewritten to name claim and field with sha256. 338 tests pass, ubuntu CI green including the ledger suite.
**Six review rounds, and the interesting output is that the phase's defect class — a mechanism reporting success about something it did not measure — appeared eight more times, four of them inside the fixes for the previous ones.** Three are one family (*the check looked somewhere the answer was always yes*): a test selector keyed on the content under test; `locate`'s correctness test whose regex hardcoded the project path, so no global claim could enter the sample; and a proposed fix that would have asserted over the real global ledger, which CI does not have — 33 claims there against 37 locally, green for the wrong reason where nobody looks. Two were mine: a diagnostic rule I specified that was a single-cause inference over a two-cause observation (the builder declined to apply it and added the leg that separated them), and a mislabelled meter, `NOT VERIFIED = 2` for a figure `ledger verify` never prints. The eighth is a new mechanism — the search ran correctly and the report of what it found was short, five dispositions accounted over six hits — which produced the durable fix: report **searched / found / dispositioned**, computed and refusing to print unless found equals dispositioned.
Filed with reproductions: #53 index-path validator (`readLedgerIndex` casts where `readGlobalLedger` validates a closed schema) · #54 view fixtures unbound to producer shape — every claim lost a required field and all 320 mission-control tests passed · #55 waiver deadlines extendable with no CI signal · #56 `c-lenses-and-playbooks-are-loaded` failing 6.6× over budget with no disposition · #57 `sweep` never consults `glob.present` while `lint` and `verify` do · #58 duplicate global claim ids accepted · #59 prose→claim citations unvalidated. **#53, #57 and #58 are one pattern — two implementations of one concept, only one carrying the check — so whatever fixes one should be tested against the other two.**
Limits, stated because a verdict that reads uniformly confident is worth less: review was **single model family** throughout, one careful opinion rather than a panel. The revert legs ran on darwin only — CI proves the tests pass on Linux, not that they fail there under the defect, and that is the one place this chain rests on argument. The belief searches were two independently-authored vocabularies over one corpus and the hit that mattered came from the second; **vocabulary search has no completion criterion — you can never say you are done, only that you stopped**, which is why #59's registration half is the only one that terminates. `craft` and `accessibility` were never applied — no browser render across six rounds.
