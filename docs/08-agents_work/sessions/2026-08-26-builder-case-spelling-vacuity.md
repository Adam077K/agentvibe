---
date: 2026-08-26
role: builder
task: case-spelling-vacuity
qa_verdict: PASS
tier: full
risk: full
branch: fix/case-spelling-assertion-vacuity
commits: 3
---
# Four assertions that deleted themselves where their premise was absent
**Chose (c) for the assigned one, and the measurement decided it.** The case test ended `if (flipped === REPO || !fs.existsSync(flipped)) return` — silent on every Linux runner. Cure is not a louder skip: the hook's actual claim ("containment is decided the way the FILESYSTEM decides it") holds on **both** kinds of filesystem, so the expectation is now **derived** — same dev+ino → ALLOW, different → BLOCK/fail-closed — and neither branch is a skip. `existsSync` was the wrong probe as well as the wrong polarity: a case-sensitive volume holding `foo` and `Foo` answers yes about two different directories. **(a) rejected with numbers:** the half the runner lacks is case-*in*sensitivity, needing `tune2fs -O casefold` or a loopback vfat mount — root, in a security test, under a sandbox where `sudo -n true` → *operation not permitted*.
**The gain is real and BOUNDED, and I only know that because I put the bug back.** With `-ef` reverted to `=`, the ALLOW branch goes red (`2 !== 0`); the BLOCK branch answers 2 either way, because `pwd -P` normalises symlinks on both sides and **case is the only axis the two operators disagree on**. So the runner went from asserting *nothing* to asserting *fail-closed* — not to covering case. `t.diagnostic` prints which branch ran, so a green CI log cannot be read as the stronger claim.
**Three more found by the sweep, all in the same file, all worse than the assigned one.** Two `KNOWN-BUG` pins keyed on `path.resolve(REPO, '..', 'ceo-1-1787176362')` — one long-gone session's directory, **absent on every machine**, so both returned silently and had been asserting nothing for as long as anyone can measure. A KNOWN-BUG pin that checks nothing makes the bug look documented. Both are **constructible** (they never ran `git`, only used a path): now built, with premises asserted, and both pinned behaviours reproduce **unchanged** — BLOCK(2) and ALLOW(0).
**Fixture location was measured, not habitual.** `os.tmpdir()` realpaths to `/private/tmp/claude-501` — byte-for-byte the scratchpad root the hook allows outright — so the ALLOW pin would have passed for the wrong reason. `$HOME` and `/tmp` are EPERM under the armed sandbox. Under REPO works in both locations. Proof: pointing the fixture back at `os.tmpdir()` fires the premise assertion by name.
**The scratchpad case is the one row where (b) is right, and the polarity is why.** `/private/tmp/claude-<uid>` cannot be created on Linux without root. The `$HOME` precedent fails on CI *because CI can build that fixture*; this one it cannot, so failing would make main permanently red for an environmental fact. Now a stated `skip` naming the absent premise and the command that ends it — and the exemption's **narrowness** (the dangerous direction) is still asserted everywhere.
**My own first control was the defect it was written to prevent.** A detector forced to `return true` passed all 168: the only negative control compared REPO with a nonexistent path, where `statSync` throws and the catch returns false — it was testing the try/catch, never the dev/ino comparison. Added `sameDirectory(REPO, path.dirname(REPO))`, two directories that both exist; the mutant now fires by name. Every claim in the new comments has a mutation that reddens it.
Verified: `test:pre-tool-use` **168 pass · 0 fail · 0 skipped** (was 167 · 0 · 0, with 4 silent returns inside them) · `npm run check` → **Tally: 43 of 43 passed · 0 failed · 141.7s**. Sweep: 13 candidates across 28 test files; 1 false positive, 4 fixed here, 7 already-stated skips, 1 reported (`run-gate.test.mjs:297`, degraded-not-vacuous — proof 1 still asserts).
