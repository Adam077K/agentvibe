---
date: 2026-08-14
role: ceo
task: mc-truncated-status
tier: lite
qa_verdict: PASS
---

`server/collectors/conflicts.ts` — a truncated `git status --porcelain` no longer invents a filename. `changedFilesFor` is async and three-state (`readable: true|false|undefined`); a partial read is signalled by the **rejection, never by inspecting the buffer**; whole-line parsing discards the trailing fragment; `readPathField` un-C-quotes; `TextDecoder('utf-8', {ignoreBOM: true})`; `line.slice(3)` replaces `.slice(2).trimStart()`. `statusConfigEnv` forces `core.quotePath=true` **and** `status.showUntrackedFiles=normal` through both `GIT_CONFIG_COUNT` and an append to `GIT_CONFIG_PARAMETERS` — git reads PARAMETERS afterwards, so it beats the COUNT pairs and a one-channel override loses.

Five defects, each the same shape — *a fabricated or missing path presented as a clean read*: C-quoted paths decoded per-escape (3 of 4 wrong); astral characters destroyed under an inherited `core.quotePath=false`; a leading BOM eaten by the decoder; `trimStart` eating Unicode-whitespace-leading names; `status.showUntrackedFiles=no` surviving the override and returning 0 records as a clean read. A sixth was found by the author while fixing the fifth: **forcing a new git config makes every test that reads bare git non-hermetic against it** — the P2 hermeticity defect reappearing in tests written after it was reported.

Suite at `8111fc4`: 222 pass / 0 fail, `tsc --noEmit` exit 0, and 222/0 under both a hostile ambient `GIT_CONFIG_PARAMETERS` and a hostile `GIT_CONFIG_GLOBAL`.

**What the PASS rests on, and what it does not.** Rounds 1–4 were single-model-family (Claude) correctness reviews; round 4 returned PASS with no p1 — **at `29c036c`**. The delta `29c036c..8111fc4` changes the mechanism (`statusConfigEnv` gains a forced key), and a prior PASS covers a later commit only when the mechanism is unchanged, so that verdict does not carry. The delta was instead verified by a **CEO-executed mutation matrix** in a `git archive` scratch: clean 88/0, hostile `GIT_CONFIG_PARAMETERS` 88/0, hostile `GIT_CONFIG_GLOBAL` 88/0, and stripping the forced setting from both channels → **11 tests red** (sha256-anchored; 0 references remained). The guard is load-bearing and not vacuous. Note the author's matrix reported 2 failures for its version of that mutation; mine deletes every line naming the setting and is cruder, so the counts are **not comparable** — this confirms non-vacuity, it does not reproduce their figure.

**The independent read did arrive, after the Founder had already authorised merging without it.** Verdict **SAFE TO MERGE**, correctness lens, single anthropic model — still not the ≥2-model-family panel the `adversarial` lens requires, and no review in this phase has been. It reproduced all five mutations at their claimed counts (2·1·2·1·1) and 221/0 in all three environments, and it answered both open questions: **overriding `=no` is the right call** — an untracked file at the same path in two worktrees is exactly Mission Control's conflict class, and the force was confirmed *not* to expose `.gitignore`d files, which bounds the blast radius — and **the two rewritten tests are honest**, the boundary test strictly more correct than before since it now measures the stream actually cut. It is **not disclosed** anywhere in `client/` or `docs/` (p3).

**Two p2 findings, neither blocking under the lens, both real and both logged rather than fixed here.** (1) **The override also clamps `showUntrackedFiles=all` downward, unstated.** Measured: repo config `all` yields `?? newdir/a.ts` and `?? newdir/b.ts` bare, but `?? newdir/` through `statusConfigEnv`; `detectConflicts` keys on the exact string with no branch between (`conflicts.ts:546-557`), so two worktrees adding *different* files under one new directory now collide on `newdir/` — **a new false positive, introduced by this fix, for `all` users only.** (2) **F3's own premise goes vacuous in an environment this PR's matrix runs green.** `collectors.test.ts:1320` reads `raw` with no env pin: 130 bytes clean, **0 bytes** under ambient `showUntrackedFiles=no`, so `0 < 2000` passes for the opposite of the stated reason and the 380 B headroom is asserted against nothing. The mutation still dies via `changedFiles.length > 0`, so this is test integrity rather than collector correctness — and it is the same shape the author fixed in two other tests *in this very commit*, left open in the one the margin was added to. Fix is one line, pinning that setting only (pinning `quotePath` would destroy the raw stream). Also confirmed: `changedFilesFor` returned byte-identical output under every ambient tried, so collector immunity is real.

Classifier floor: `lite` (`matched: (none — default)`) — no `qa-tier-floor.yml` rule mentions mission-control, so the tier was raised to full by hand, which is not a mechanism; logged as #34·#35. Open, not fixed here: **#46** (assert `parseStatusPorcelain` against `git status -z`, so the fixture stops deciding coverage) and **#47** F1 (fixture spawns inside an unwritten 5 s budget).
