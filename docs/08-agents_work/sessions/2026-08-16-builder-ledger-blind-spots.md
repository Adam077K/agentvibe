---
date: 2026-08-16
role: builder
task: ledger-blind-spots
tier: full
qa_verdict: PASS
---

Three ledger defects of one shape — **two implementations of one concept, only one carrying the check** — fixed on `fix/ledger-blind-spots-2`, one commit each. **#57:** `sweep` printed a bare claim total where `verify` declared the absent global ledger, so on a CI runner four global claims left the set silently and a lapsed waiver on one was caught by nothing; the sentence now lives in one function both call, the total splits by scope, and absence is declared without being counted as a finding — a job that goes red every day on a fresh runner is a job nobody reads. **#58:** the global YAML accepted two claims at one address because `validateClaim` is a closed per-entry schema and cannot see a collision between entries; `globalClaimLine` had already measured the duplicate and discarded it by returning null, so it is split into a form that returns every match, and the message names both line numbers or says the positions could not be measured.
**#59** is the one with leverage: nothing validated a claim id cited in prose, so the reference pattern — the only documentation habit with a completion criterion, since `git grep` is exact where a vocabulary search is only ever stopped — could rot the moment anyone leaned on it. Deterministic scan wired into `lint`, never a model sweep (Rule 10: a semantic pass can report `unresolved`, never `pass`). **A citation is an id in inline code, alone in its span, outside every fence and outside frontmatter** — measured, not assumed: 83 `c-` tokens sit outside fences repo-wide and 80 are backticked, and two of the three that are not are the English word "c-suite", which matches the id grammar exactly. **80 citations resolve; 8 cannot and are declared with a reason each** — three are real claims living in machine state no runner has, five name ids nobody registered. The exemption list is a ratchet: an entry nothing cites fails, an entry that became a real claim fails, and an entry claiming global scope that the machine's global ledger lacks fails, so it can only shrink.
Every fix has a constructed failure and a test that fails when the fix is reverted — each check was mutated in turn and the matching tests went red, including one that had to be rewritten after mutation showed it was asserting absences a dead scanner would satisfy. **86 tests pass** (78 → 80 → 86 across the three commits); `npm run check:ledger` exits 0; `ledger verify` reports **69 pass · 5 would_block · 0 block** with the events log pointed at a path that does not exist.
Two measurement notes worth keeping. `WARROOM_EVENTS=$(mktemp)` **creates an empty file**, which makes the log *present but empty* — the sweep then files every canary-covered resolver as silent, the claim whose evidence runs `ledger sweep` exits 1, and the would_block count reads 6 rather than 5. Point the variable at a path inside a temp dir instead. Separately, the mission-control cold-start claim is a **10,000 ms budget this machine straddles** (measured 9.7 s and 15.2 s minutes apart), so it flakes in and out of the count; unrelated to this work. Issue #56 is untouched — session-start output is byte-identical at 27,069 before and after.
**#55 not attempted, as briefed** — recommendation returned separately: cap total deferral rather than index the disposition.
