---
date: 2026-08-28
role: builder
task: block-scalar-content
qa_verdict: PASS
tier: irreversible
risk: irreversible
branch: fix/block-scalar-content
commits: 11
---
# A block scalar is content, and the parser was reading it as a stream of scanned lines

**The root cause is a whole-document pre-pass, not a line-level bug.** `scanLines()` normalises every line of the document before any structure is known: it drops blank lines, drops `#`-first lines, runs `stripComment` on every line, right-trims, and strips all leading indentation. All five are correct for a plain scalar and all five destroy a block-scalar body, which the `>`/`|` branch then reassembled from that already-normalised stream. **Two of the five delete the line from `lines[]` entirely**, so the body was not reconstructible from the scan's own output — which is why the fix reads back into the raw source and every scanned line now carries its raw index. Five of SIX content losses fixed: a `#` preceded by whitespace truncated the value; a `#`-first line vanished; a blank line vanished, and in `>` that destroys the fold boundary so `alpha\nbravo` became `alpha bravo`, a different sentence; relative indentation flattened to column 0; trailing whitespace was stripped. **The sixth is named and NOT fixed**, because it is upstream of this fix and not reachable from it: `scanLines` runs `stripComment` over block-scalar bodies too, so an odd apostrophe throws `unterminated quote` and takes the **whole document** with it, not one value. `.claude/agents/reviewer-readonly.md` frontmatter is the live instance — unparseable by the repo's only YAML parser, with no live consequence because `schema-lint` reads frontmatter with its own `parseFrontmatter`. This fix makes the block-scalar **value** content; the **admission** decision is still the line scanner's.

**Differential result, with its denominators.** 32 probe cases against **PyYAML 6.0.3** and **js-yaml 4.1.1**, the two reference arms agreeing on **all 32** — so no case is ambiguous and no verdict rests on one implementation's quirk. Before: 11 AGREE. After: **18 AGREE · 11 DIFFER · 3 OURS-THROWS · 0 SUBSTANTIVE.** Every one of the 11 diffs is the deliberate trailing `\n`; all 3 throws are the deliberate `|+`/`>+`/`|2` refusal. Live corpus of 44 documents that real consumers feed the parser: **1 SUBSTANTIVE before → 0 after**, and `.claude/gates.yml` stayed at exactly **15** trailing-newline-only, which is the control that says nothing there was broken. *An instrument note worth keeping: the first corpus run reported 76 SUBSTANTIVE findings and 75 of them were js-yaml resolving ISO dates to `Date` while the other two arms return strings. A normaliser fixed the instrument, not the parser. A sweep that has never been calibrated against its own reference arms will hand you a plausible number.*

**The live block-scalar surface is TWO files, not thirteen.** Enumerated with `grep -rn "parseYamlSubset" --include=*.js --include=*.mjs --include=*.ts .` (globs quoted — unquoted, zsh eats them and returns a false 0), then each call site read for the file it opens, then each of those files parsed by all three arms. Only `.claude/gates.yml` (15 block scalars across 8 keys) and `.claude/skills/CURATION.yml` (`added[].why`) carry any. Everything else the parser is fed — `ledger.mjs:250`, `classifier.js:87`, `events.js:39`, `vendor-provenance.mjs:56`, the six playbooks, both lens files, all command frontmatter — **was measured and holds zero block scalars: no-change rows**, reproducible with the same sweep. Agent frontmatter carries 23 block scalars but is **not** on this path: `schema-lint.js` parses it with its own `parseFrontmatter` (line 173).

**RESIDUAL — `.claude/gates.yml`'s consumers survive by coincidence, and nothing pins the coincidence.** Every consumer of those 15 values in `check-gates.mjs` is `.trim()` plus a **minimum** length: `summary.trim().length < 20`, `recorded_in` `.trim().length < 4`, `hasSubstantiveReason(unused_reason)` against `REASON_MIN_LENGTH`, and `String(recorded_in).trim()` on print. A trailing `\n` cannot push a value *below* a floor, so no existing test would notice if chomping changed. **That is a property of today's predicates, not a guarantee.** The shape of the future break: any `===`, `startsWith`, `endsWith`, `split('\n').length` or hash over one of these values, added by someone who has no reason to know the parser strips a newline other parsers keep. It will be silent.

**`CURATION.yml added[].why` was live data loss — and it has no live consumer.** Re-derived, because the figures in the brief measured the wrong thing: before **1118 chars · 15 lines**, after **1255 chars · 15 lines**, reference **1256 chars · 16 lines**. The **line count of content never changed**; 16-vs-15 is the trailing-`\n` split artifact. The real damage was **137 characters of relative indentation flattened to column 0**, which is what turned the two-column skill-creator / writing-skills reversal record into unreadable prose. Both halves matter: the corruption is real, and `grep -n "\.why\b" scripts/curate-skills.mjs scripts/build-skill-routers.mjs` returns nothing — **neither consumer reads the field.** So this is a fixed corruption in a human-read record, not a fixed production bug, and saying only the first half would overstate it.

**Two enumeration corrections to the brief, so the next person redoes them rather than trusts them.** `scripts/gates.test.mjs:497` and `scripts/ledger.test.mjs:2202` are **comments, not calls** — neither file invokes `parseYamlSubset`. And `.claude/hooks/schema-lint.js` has **6** call sites (492, 1481, 1590, 1766, 1778, 1790), not 5. Both found by reading every line the grep returned instead of counting the grep's hits, which is the difference between a census and a tally.

**Chomping stays `strip`, and it is a decision now rather than an accident.** YAML's default is `clip`; this parser applies `strip` to all four indicators it accepts. Kept for three measured reasons: `|-` and `>-` already match both references byte for byte, so adopting clip would make `|` and `|-` **diverge where they agree today**; every value it would change sits on an `irreversible`-tier surface and the change recovers **zero** content; and it is one of the divergences enumerated in `claims.js` rather than "the only remaining gap", which is what an earlier draft of this file and of the source comment both claimed. **That sentence was false in four ways and nothing went red when it stopped being true** — the review found four behaviour-changing mutants that no test killed, and the claim was contradicted by a test twenty lines below it in the same file. It is replaced by a numbered list in `claims.js`, each row carrying a cross-check assertion: clip chomping; `|+`/`>+`/`|2` silently yielding the **literal indicator string** when there is no body (`{a: "|+"}` against the references' `{a: ""}`) rather than the refusal the old test claimed; an empty block scalar throwing where the references return `""`; and the sixth loss. **The refusal fixture was shaped not to see its own failure** — all three of its cases carried a more-indented body, which is the only shape in which the refusal happens.

**A fixture built from the fix cannot fail, so it was mutated.** Restoring **`62e0c26:scripts/lib/claims.js`** — cited by ref, not by offset, because `HEAD~1` named the pre-fix blob only on the day it was measured and now names a fixed one: **62 pass · 10 fail**; restore → **72 pass · 0 fail**. A second lane measured 63/9 on the same blob, and the one-test gap was itself the E5 defect below: with `js-yaml` unreachable the cross-check `return`ed and counted as a pass. The 4 new tests that pass under both are exactly the ones pinning behaviour that did not change (plain scalars, strip chomping, the refusal, the empty-block throw). Two controls, one per arm: a positive control spelling out the pre-fix output and asserting the parser no longer returns it, and a plain-scalar control that fails if the fix had been implemented by weakening `scanLines` instead of reading past it. The defeat fixture is a body line indented *between* the parent and the content indent — neither body nor sibling — which the pre-fix parser accepted and this one refuses.

**A test's own dependency is a third state.** `package.json` declares **zero** dependencies; `js-yaml` resolves only from a developer's home and is absent on a runner, so the brief's instruction to compare against a reference implementation was unsatisfiable as written — a live-only check would be a test that passes by being absent. Resolved as pinned PyYAML literals (with the derivation command in the file) **plus** an opportunistic live cross-check that reports **UNRESOLVED by diagnostic** when no reference is reachable. It can only add a failure, never remove one.

**One line fixes two DIFFERENT findings, and merging them would be the conflation this repo has already been burned by.** Measured on three arms with blob shas pinned — base `62e0c26` → `f5172d9`, round 1 `6540d1e` → `6764c05`, head — against js-yaml, with an LF control agreeing on all three:

| input | base | round 1 | head | provenance |
|---|---|---|---|---|
| `a: \|-\n  x\n  y` (LF control) | AGREE | AGREE | AGREE | — |
| CRLF | **AGREE** | **DIFFER** | AGREE | **REGRESSION INTRODUCED BY THIS DIFF.** This is what blocks |
| lone-CR, block scalar | DIFFER | DIFFER | AGREE | **PRE-EXISTING.** The diff neither caused nor fixed it |
| lone-CR, plain scalar | DIFFER | DIFFER | AGREE | **PRE-EXISTING**, and needs no block scalar to show |

**Round 1 broke CRLF and I did not notice; the adversarial lane did.** Base agreed with both references and round 1 did not — a regression, with the obligations of one. **lone-CR is a different animal**: byte-identical on base and head-before-this-round, so it belongs with the out-of-diff class beside the unterminated-quote loss. `scanLines` splits on `\n` only, so a CR-terminated file is ONE line and the block indicator itself becomes the value — `{"a": "|-\r  x\r  y"}`. It is also **the cheapest counterexample to the "only remaining difference" sentence**, because the plain-scalar row needs no block scalar at all: `a: 1\rb: two` was one line and one wrong value, on `main`, today.

The one line cures both. Three candidates measured: doing nothing leaves both; the narrow `/\r\n/g` fixes only the regression; the wide **`/\r\n?/g`, once in `parseYamlSubset`**, fixes all four rows. It removes the **condition** — two blankness predicates over one source — rather than a consequence. Live scenario for the regression: a contributor commits `gates.yml` with CRLF, a whitespace-looking diff, every one of the 15 `>` values silently carrying interior `\r`, `check:gates` still exit 0, and the printed governance record rendering only the last physical line.

**The normalisation was then shown NOT to move the paths it should leave alone.** Exhaustive fuzz over an LF/space-only alphabet — **1600 documents**, 4 indicators × 400 bodies, enumerated to depth 3, asserted CR-free by construction — comparing head against head-with-the-normalisation-removed: **no-op on 1600/1600, zero differences.** Positive control on the arm that can fail silently: the same comparator reports a difference on a CRLF document, so it is not blind. Comparing round 1 to head over the same 1600: **224 behaviour changes, and 224/224 contain a whitespace-only body line** — every single one attributable to the A2 fix and nothing else.

**A test's `t.skip` is not decoration.** The cross-check used `return`, which renders `✔` and counts in the pass tally — so on CI, where `js-yaml` is *always* unreachable (zero dependencies, no lockfile, no install step), an absent reference read as a satisfied check. Proved end to end by hiding `js-yaml` from module resolution: hidden → `# SKIP` carrying the reason, **76 pass · 1 skipped**; present → **77 pass · 0 skipped**.

**The four survivors the review used as evidence for E1 were re-derived, not assumed: three are now killed and ONE still lives.** M1 (`isBlank` → `/^\s*$/`) KILLED · M1b (`isBlank` → `/^[ \t\r]*$/`) KILLED · M17 (whitespace-only line → `''`) KILLED · **M15 (strip a trailing `\r` from body lines) SURVIVES.** Saying so rather than adjusting the claim is the point of running it. M15 is **equivalent**, not uncovered — the distinction I got wrong on M1 one round ago, so this time it was measured: head versus head+M15 across **3,284 documents** (the LF alphabet, plus CR-bearing bodies the LF alphabet cannot produce, plus four explicit CR documents) gives **0 behavioural differences**, while the same comparator finds **1,552** differences against a known-different arm. No `\r` can reach `readBlockScalar` because `parseYamlSubset` normalises before it splits, so the mutant deletes something that can no longer occur. **A surviving equivalent mutant is the fix landing upstream of where the mutant operates** — which is what "removes the condition, not the consequence" means, stated as a measurement instead of a slogan.

**Mutation battery, with both controls behaving.** Positive control (fold space → underscore) KILLED at fail=3; negative control (a no-op comment) SURVIVED. Killed: narrowing `\r\n?` to `\r\n`; removing the normalisation; `blanks + 1` → `1` in `foldLines`; a whitespace-only line pushed as `''`. **`isBlank` → `/^\s*$/` survived the first run**, which meant it was *uncovered*, not equivalent — both parsers refuse a tab-only body line and nothing pinned it. A test pins it now and the mutant is killed. The `headerRaw` guard is kept and **labelled as unreachable from any document**, with the mutation result written beside it, so nobody claims coverage it does not have.

**Verified:** `node --test scripts/claims.test.mjs` → **80 pass · 0 fail · 0 skipped**. `npm run check` → **`Tally: 48 of 48 passed · 0 failed`**, **exit 0**, 182.0s wall clock cross-checked against UTC stamps, sandbox armed. Denominator derived, not quoted: `node -e "console.log(require('./scripts/lib/check-suite.js').STEPS.length)"` → 48; no new `check:*`/`test:*` name was added, so `STEPS` and `EXCLUDED` are untouched. Tier from `node scripts/classify.mjs scripts/lib/claims.js scripts/claims.test.mjs`, not assumed: **floor `irreversible`**, driver `scripts/lib/claims.js` → `scripts/lib/**`. Base re-derived twice during the task — `origin/main` moved `d1294a4` → `b197048` → `62e0c26` — and merged in both times, never rebased; zero overlap with the changed files on either move, so no verdict was invalidated.

**A THIRD ROUND, and the finding is that I made the same mistake twice with a bigger number.** Round 2 was commissioned to remove one unqualified totality claim — *"the trailing newline is the only remaining difference"* — and the replacement was another one in the same block with stronger emphasis: *"FOUR DIVERGENCES REMAIN … NONE is a sentence without a test behind it."* The delta review found **two more**, both verified here with controls: a block scalar whose leading all-space line is wider than the content indent produced `{"k":"   \na"}` from a document **both references reject** — a **fail-open**, this diff's own doing — and a block scalar as a bare sequence item throws where the references read a one-string list (pre-existing, fail-closed, named and deliberately not fixed).

**The durable part is structural and it is the reviewer's, not mine: the cross-check list is a ONE-DIRECTIONAL control.** It fires when a **listed** row stops diverging. **Nothing fires when a divergence is missing from the list** — and that is the direction that failed both times. So the very mechanism I built to pin the enumeration can only detect over-listing, never under-listing. Two routes were open. **I took scoping, and the reason is a measurement, not economy:** the bidirectional control — a sweep that fails when *any* unlisted divergence exists — needs a reference parser to compare against, and this repo declares **zero dependencies**, so no reference resolves on CI. Such a sweep could only ever be developer-run and advisory, never blocking. A blocking control that cannot run is the thing this repo calls a wish. The comment now claims what has been enumerated and measured and **explicitly disclaims exhaustiveness**, and it says why in place, so the next person does not attempt a third totality claim.

**The fail-open was CAUSED by a false general rule, which is why the sentence and the code were narrowed together.** *"Real YAML keeps whatever sits at or past the content indent"* is true for interior and trailing lines and **false for a leading empty line**, and the code applied it unconditionally. Fixing only the sentence would have left the fail-open; fixing only the code would have left the next reader with the broad rule. Two controls are pinned beside the new refusal — a leading blank at exactly the content indent, and a shallower one — both of which the references accept and both of which still pass, so the refusal is narrow rather than a ban on leading blank lines. **Live incidence of the fail-open was 0**: 48 block-scalar headers in the tree, none with a whitespace-only first body line.

**A changed class that had been travelling unmentioned.** `k: 'a\rb'` now **throws** where base and round 1 both returned `{"k":"a\rb"}` and both references give `"a b"`. It is the same quote-tracking mechanism as the sixth loss, reached by a different route, and it is a **side effect of the line-ending normalisation** rather than something the round set out to do. Fail-closed and defensible for a quoted scalar carrying a stray CR — but it changed behaviour with no test, so it has one now, in both the single- and double-quoted forms. The double-quoted form was not in the review and behaves the same way; I found it by testing the class rather than the instance.

**Mutation, third round, both controls behaving.** Removing the leading-line guard KILLED (fail=2) · loosening `>` to `>=` KILLED · `src[k].length` → `indentOf(src[k])` **SURVIVED and is equivalent** — 0 differences across **6,666** documents against **1,524** for a known-different control arm, because `isBlank` guarantees the line is all spaces and the two are then equal by construction. Measured rather than argued, because "provably equivalent" is exactly what I wrongly assumed about `isBlank` one round ago.

**Two corrections to the review, both minor and both measured.** Its live-corpus figure of 44-not-39 is **arithmetically right and its framing is not**: parsing `.github/workflows/qa-lead-pass.yml` whole does yield 5 more trailing-newline-only findings, but **nothing in production feeds a workflow file to this parser** — `extractClaimBlocks` finds zero claim blocks in it. So 39 is correct for the corpus as defined (documents real consumers feed the parser) and 44 for a wider one (every YAML document in the tree, parsed whole). Either way `0 SUBSTANTIVE` holds, which was the reviewer's point and it stands. Second: C-2 was filed against the single-quoted form only; the double-quoted form diverges identically.

**Round 2 corrected this file as well as the code, and that is the finding.** Both p1s from review #121 were **sentences, not code** — the diff claimed more about itself than it had pinned, in a repo whose whole thesis is that an unenforced sentence is a wish. One of them was refuted by a test inside the same file. A third correction was self-inflicted and caught before commit: fixing A2 removed a divergence, so the enumeration this round *added* was briefly wrong at five, and had to be re-measured down to four before it shipped.

**Not done, named rather than omitted:** the `mission-control/server/lib/claims.ts:55` re-export guard. It is another session's surface and the guard could not be expressed from `scripts/` without writing there, so it was left. **Standing caveat:** this PASS means *the checks ran and are green*, **not** that the tier was satisfied — `irreversible` asks 2-of-3 multi-judge across ≥2 model families, no non-Anthropic model is reachable inside Claude Code, single family. Accepted risk, exit condition **2026-11-17**.
