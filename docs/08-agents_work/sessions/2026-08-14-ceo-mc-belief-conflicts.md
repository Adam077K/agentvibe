---
date: 2026-08-14
role: ceo
task: mc-belief-conflicts
tier: full
qa_verdict: PASS
---

PR4: the view registry, Belief over both ledgers banded by scope, and Conflicts — plus the three defects Conflicts already had on `main`. Reviewed at **Full tier under three lenses** (correctness · security · craft), Claude-only, so **independence is NOT met**: `evidence` and `adversarial` are `independent: true` and want ≥2 model families. Every lens returned **FAIL** on the first pass. Fourteen defects, four fix rounds. The classifier returns `tier=lite, matched=(none — default)` for every mission-control path — no rule mentions it (#34) — so this tier was chosen by hand, which is not a mechanism.
**What the review bought, that four earlier rounds missed:** three CONFIRMED RCEs on `main` (#36, git `core.fsmonitor` via the sweep; `node <discovered-project>/scripts/ledger.mjs` via belief; a claim's `evidence.cmd` reaching `/bin/sh -c`) — reported because the PR's security claim was that a narrowed source-text guard plus one behavioural test cover this route, and all three are invisible to both. **"`server/**` invokes no shell" is literally true and operationally void**: the shell is one `execFile` away, in a program the guard does not scan, reached with caller-controlled data. That is the wrong invariant, defended for three PRs.
**The §0 class appeared five more times, four of them inside this PR's own test code** — the barrier asserting on `process.cwd()` while the code under test passes `cwd: worktreePath`, certifying a live RCE as safe; the guard matching no `execFileAsync`, the alias it blesses; a prefix test that passed under all three mutations and had never once fired; a sampler whose validity depended on the machine being slow enough to observe; and an instrument that drained its own measurement as microtasks before the timers phase, reporting 2.4 ms against an implementation blocking 71 ms.
**What produced those findings was procedure, not model.** Reviewers were told to execute rather than reason, to attack a named claim first, and that a clean report was acceptable. Fixes were accepted only on **proof-by-reversion** — show the new test failing against the vulnerable implementation. That demand is what surfaced every one of the five. Two of my own figures were wrong and a worker caught each: 4 global claims not 5, and `/api/belief` at 10,385 ms not the 25 ms I put in a brief, where I had timed a `MODULE_NOT_FOUND` crash without checking its exit code.
**Disclosed, not hidden:** mutation B is bound behaviourally by a 12-project fixture the reviewer built after disproving the builder's claim that no timing test could — both terms scale linearly with project count, so the ratio is scale-invariant and the fixture was the wrong *shape*, not too small. The structural pin is kept for the call-site revert only and its comment now says so; the reviewer defeated it three tsc-clean ways, two of which are what PR5 unifying the two listers looks like. The 0.75 bound names the quantity that decides it — `spawn/(spawn+wait)`, 0.037 here and 0.460 on CI — so its residual risk is false-RED, not false-green. Verdict is CEO-declared on reviewer PASS under the Founder's rule-8 widening for PR4/PR5 only; #24 stands.
