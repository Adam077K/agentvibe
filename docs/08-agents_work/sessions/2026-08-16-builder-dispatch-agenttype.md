---
date: 2026-08-16
role: builder
task: dispatch-agenttype
tier: full
qa_verdict: PASS
---

**Eight of twelve `agent()` dispatch sites were not in a container.** Measured, not estimated: `coding.js` defaulted to `backend-engineer` (no such file since Phase 4b) *and* forwarded its caller's `agentType` unvalidated; `design.js` named `product-designer` and `design-critic` (neither exists) and left the synthesis site bare; all four `research.js` sites named the `researcher` shim or nothing. A missing name, a phantom name and a `kind: shim` name all resolve the same way — `general-purpose`, tools `*`, holding Write, Edit and Bash. So the design critic could edit what it graded, and the four containers that read the open web held a shell.
**Repointed with the reason kept at the call site**, since the reason is the part that rots: explore → `designer` (the only engine with `playwright` *and* `Write` — that phase must render something and then look at it); critique and synthesize → `reviewer-readonly` (both consume serialized JSON and emit through a schema, so neither needs a pen or a shell); all research → `sourcer` (`Read, Glob, Grep, WebSearch, WebFetch`, and nothing else, which is the point). `coding.js` now carries `const DISPATCHABLE = Object.freeze(['builder','designer'])` and refuses anything outside it *before* dispatching — forwarding a caller-chosen agent name is dispatch-identity injection, and it gets worse the day `operator` exists to be named. Also deleted `research.js`'s *"Use Context7 for library docs first"*: no `context7` server exists at any scope, and an instruction naming an absent capability is the same defect class as an `mcpServers:` declaration nothing backs.
**`scripts/check-dispatch-agenttype.mjs` is the regression test** (CONTROL-PLANE §3.16, §3.17 Step 3), blocking in `ci.yml` via `npm run check:dispatch`. It refuses a missing `agentType`, a phantom or shim name, a caller-supplied name with no frozen and actually-tested allowlist, and a credentialed `agentType` outside `.claude/workflows/` (vacuous today — which is when it is cheap). Textual scan, because these files are ESM fragments with runtime-injected globals that neither `import()` nor `vm.Script` will load; the tokenizer tracks strings, templates, `${}` and comments because `qa.js:283` nests `${… || '…'}` inside the prompt argument. **Two of my own three parser bugs were found by running it, not by reading it** — a comment above `agentType:` swallowed the key, and `stripComments` failed to pop the brace `${` pushes, so a template's closing backtick read as an opening one.
**Non-vacuity floor: 12 sites, asserted.** A parser that quietly stops finding dispatches must fail, not report clean. The 23 shipped tests construct every defect rather than trusting the rule fires, including two mutations that delete `agentType` from a known-good site — one of them the template-literal trap — and assert the flag.
**Verified:** checker exits 1 with all 8 findings before the repoint, 0 after; `node --test scripts/check-dispatch-agenttype.test.mjs` 23/23. **`npm run check` is red on `check:mc` for a reason this diff did not cause:** `ledger verify --offline` is non-deterministic on a loaded machine — three consecutive runs gave 69/5, 69/5, 68/6, the flip being `c-mission-control-cold-start [claim-command]`, whose own output reports `cold-start rate HIGH: 3330ms/GB` at load average 10.5. `.claude/workflows/design-screen.md` documents four more phantom dispatches in prose and is checked by nothing; out of scope here.
