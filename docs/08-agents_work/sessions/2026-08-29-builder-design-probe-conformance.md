---
date: 2026-08-29
role: builder
task: design-probe-conformance
qa_verdict: PASS
tier: full
risk: full
branch: feat/design-probe-conformance
commits: 4
---
# The probe stopped holding an opinion about type and started asking the token file
**Deleted a rule this repo invented and the research falsified, and recorded the deletion at both sites.** `MIN_STEP_RATIO = 1.125`, `scaleGaps()` and the `type-scale-near-duplicates` finding are gone. The falsification is not a matter of tuning: a UI band on a constant integer increment has `ratio = 1 + d/s`, which **decreases monotonically** (11→12 is 1.091, 14→15 is 1.071), so a flat ratio floor condemns exactly the construction it was written to reward — and linear.app, stripe.com and vercel.com all violate it. The test that pinned the boundary in both directions is deleted with it, because a test that pins a falsified rule makes it *harder* to remove. Replaced its job, not its opinion: `token-conformance` reports every rendered `font-size`, `line-height` and `letter-spacing` absent from `design/tokens/tokens.json`, with its usage count and the nearest token, citing the token file **by path**.
**A silence is never a pass.** A token group the file does not declare produces **no finding and an `unchecked` entry** naming the absence. The real token file declares `fontSize/lineHeight/letterSpacing` and **no motion at all**, so motion conformance is UNCHECKED against it today and says so on stdout and in the artifact. `--out` writes JSON carrying findings AND `unchecked`; a **REFUSAL is written into it as `exit: 2`**, so a reviewer with no browser and no shell cannot read an empty findings list as a clean run.
**Found by running it, not by reading it: the authored easing is not in one place.** Measured in Chromium, one page, one `getAnimations()` call — a `CSSAnimation` carries it on `getKeyframes()[].easing` while `getTiming().easing` reads `"linear"`; a `CSSTransition` is the **exact opposite**. Reading either alone returns a *plausible wrong answer* for half of all motion. The interpretation moved out of `collect()` (serialised into the browser, untestable) into `resolveMotion()`/`authoredEasings()` in node, and the raw capture is pinned as a fixture.
**Verified by breaking it, 19 mutants, one at a time:** delete any single check and a named test goes red — **19 of 19 killed**. One survived the first round (the per-keyframe arm for script-driven `Animation`s), which was a genuinely unguarded line; it has a control now and the round was re-run clean.
**THE NAMING CONVENTION AND THE TIER SYSTEM POINT OPPOSITE WAYS, AND TWO BUILDERS HIT IT IN ONE AFTERNOON.** The suite's `GOVERNED` predicate is `/^(?:check|test|lint|verify|audit):/`, so calling this file's tests `test:design-probe` — the obvious name, and what every peer test is called — makes the script *governed*, which then demands either a STEPS entry (needing a CI-workflow counterpart, `irreversible`) or an `EXCLUDED` entry in `scripts/lib/check-suite.js` (itself `irreversible`). Either path prices a probe rework at founder sign-off, **with no warning at the point of naming**. I took the `EXCLUDED` route first and it set this branch's floor to `irreversible` for 27 lines of prose the audit never asked for; `builder-tokens` hit the identical trap the same afternoon reaching for `check:tokens`. The cure is the landed precedent `b1ab4ce`: `scripts/design-probe.test.mjs` rides `test:probe-readonly`'s argv, `design-probe` stays an unmatched plain script needing no entry, `STEPS.length` is **48 before and after**, no workflow file is touched — and the probe's arithmetic now actually runs, where the exclusion bought a written explanation for zero coverage. The prose was **relocated, not deleted**: the SIGTRAP measurement, the three exit states and *"it is not silently skipped"* live in `design-probe.mjs`'s own header, where they cost nothing. The counterweight assertion in `check-suite.test.mjs` (tier `full`) buys back the one guarded position piggybacking gives up — `test:check-suite` pins STEPS against script *names*, not their argv, so a deleted filename would take its tests with it and stay green.
**The "more coverage" claim is TESTED, not asserted — three cells, one defect.** Suppressed `token-conformance` findings in `findingsFor()` (a real defect the check exists to catch) and measured what notices. **Cell A, as wired:** `npm run test:probe-readonly` → **exit 1, 5 tests red**. **Cell B, the control — same defect, `design-probe.test.mjs` removed from the argv, which is exactly what the `EXCLUDED` arrangement gave:** → **exit 0, 32 pass, caught by nothing**. **Cell C, end to end:** `npm run check` → **47 of 48 · 1 failed · `✗ 40. test:probe-readonly`**. Then restored, working tree byte-clean against HEAD, and the suite back to 48 of 48. Cell B is the load-bearing one: without it, "the argv route gives more coverage" is a sentence rather than a measurement.
**Verified:** `node --test scripts/design-probe.test.mjs` → **43 pass · 0 fail**; `npm run test:probe-readonly` → **75 pass · 0 fail** (32 + 43 under one name); `npm run test:check-suite` → **74 pass · 0 fail** with `check-suite.js` byte-identical to `origin/main`, which is what proves `design-probe` needed no entry. Suite: `npm run check` → **48 of 48 passed · 0 failed · exit 0**, sandbox armed, exit code read directly and not through a pipe. Probe CLI both directions: sandboxed **exit 2** with a `REFUSED` artifact; escalated **exit 1**, 5 viewports, 14 p1 + 10 p2 findings, `getAnimations()` returning the authored `ease-in-out`. Tier from `node scripts/classify.mjs` over the final diff, not assumed: floor **full**. Standing caveat — this PASS means the checks ran green, not that the tier was satisfied; single model family, accepted risk to 2026-11-17.
