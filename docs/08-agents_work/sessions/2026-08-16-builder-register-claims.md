---
date: 2026-08-16
role: builder
task: register-claims
tier: full
qa_verdict: PASS
---

Seven durable facts left prose and became `verified_by: command` claims, closing STATUS.md open items 1 and 2 — item 2 named its own hazard: *"durable facts live only in prose, which is exactly where the `maxTurns` belief lived while it was wrong."* None is a `judge` claim: an empty panel resolves `unresolved` forever, two already sit in that state, and a third would register a fact as a permanent would_block.
**`c-read-only-binding-verified-by-attempt` is the one that mattered** — `tools:` subtraction verified by *attempt*, not argument. `reviewer-readonly` reported write/bash/edit as **`NOT_PRESENT`**, strictly stronger than `PRESENT_BUT_REFUSED` (which would mean the capability exists and only a hook stands in the way), with a control read that succeeded, proving the probe could act — a silent probe and a contained probe leave the same transcript. **Bounded in the assert:** measured on the `Agent` path; `qa.js` dispatches its judge through `agent()` on the Workflow surface, probably the same mechanism, not measured.
**`c-read-only-binding-unverified` is DEPRECATED, not refreshed, and that was a deliberate departure from the brief.** You cannot refresh *"whether it binds is unverified"* with evidence that it IS verified — the new evidence is the negation of the claim, not a renewal of it. Mechanically it also matters: `refresh` does not short-circuit the resolver (`resolvers.js:83`), so on a `judge` claim with `judged_by: []` it books a permanent would_block nobody can clear, since no panel will convene to judge a sentence already known false. `deprecate` keeps the claim text verbatim — the record that it sat open since Phase 4 — and resolves clean. Measured: 6 would_block either way with deprecate, 7 with refresh.
**`c-tools-subtraction-binds` and `c-tools-addition-does-not-bind` are two claims, not one**, because the halves fail differently: subtraction is the security property the gate rests on (4,373 reviewer calls, 0 Write, 0 Edit), addition is a warning that `reviewer` declares four tools and emitted `StructuredOutput` in 259 of 269 runs. `tools:` is a **ceiling on removal, not an inventory**.
**`c-mcp-grant-binds-through-agent-dispatch`** settles what `GRANT-HOLDERS.md` §3.7, ROSTER-SIZE F1 and §8 X2 left contested (`designer` 24 `mcp__playwright__*` tools, `builder` zero). Its command checks the **configuration the measurement was taken against**, not the dispatch — a resolver cannot spawn an agent, and a command implying it re-measured would be the more expensive lie. Same discipline on the tools claims.
**`c-maxturns-binds-when-agenttype-named`** is bounded in its own assert (13 of 20 dropouts explained; 7 recorded 21-34 calls and are unexplained) and forced a correction: `CLAUDE.md` said the cap "is 30 on every engine" — `framer` and `sourcer` declare 25. **It also contradicts `AGENT-ARCHITECTURE.md`'s binding table, which says `maxTurns:` does NOT bind.** Both cannot stand; that file is the truth lane's, so it is flagged in STATUS.md rather than edited.
**`c-mcp-hook-matcher-must-name-the-tool`** (line 562) is deliberately a change-detector, pinning the matcher *and* the allow-anything fall-through. When the hook policy lands the underlying fact changes from *"every other MCP tool is unhooked"* to *"an `mcp__*` payload reaches a policy decision"* — a **rewritten claim, not an edited one**.
**`c-nested-subagent-spawn-works` shipped FAILING and went green when #63 landed**, which is the whole argument for a claim over a paragraph: it was refused a green tick it had not earned, then earned it without anyone editing the claim. Weakening the command would have destroyed exactly that signal. Issue **#56** decided by disposition on the existing claim rather than a duplicate: 27,069 bytes against a 4,096 budget, `refresh` — shrink the payload with a router, do not raise the budget.
Stale figures corrected: the permission model is **39** rules (29 allow, 10 deny), not 26; the roster is **18** agent files, not 17 (`reviewer-readonly.md` landed in #47).
**Registered `c-effort-frontmatter-binding-unverified` to fix a break neither PR contained.** #64 cited that id in `PROMPT-STANDARD.md:526`; #66 added the checker that resolves prose citations. Both are correct alone and red together — `ledger lint` failed on `main`, on files byte-identical to `main`. The claim is checkable precisely because the channel is unexercised (zero agent files declare `effort:`) and it fails the day one does, which is when it must be re-decided.
`ledger verify` **69 pass / 5 would_block / 0 block** before, **85 pass / 5 would_block / 0 block** after merging `main`, exit 0 both times — all eight new claims pass and the ledger's remaining would_blocks are the pre-existing set (canary x2, two unjudged judge claims, #56). Recorded in `CLAUDE.md`: `ledger verify --offline` takes 49.6s idle and 65-67s under parallel-lane load against mission-control's hard 60s `VERIFY_TIMEOUT_MS`, so `crosscheck.test.ts` and `npm run check` fail on a busy machine — reproduced at **66.68s on a stashed clean baseline**, so it is not the claims, which cost 0.35s of the total. Docs and ledger only. Author-declared verdict, no independent reviewer — #24, and the discretion it names.
