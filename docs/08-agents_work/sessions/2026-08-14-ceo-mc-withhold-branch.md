---
date: 2026-08-14
role: ceo
task: mc-withhold-branch
tier: full
qa_verdict: PASS
---

The stall pin's **withhold branch had never fired** — not in 20 clean runs, not in any of eight mutations — while guarding whether a merge gate is allowed to speak, and its own assumption (`amax < 2 × fmax` for correct code) was already violated in 3 of 20 clean runs, held quiet only by a `lineMs/fmax` margin of 5.4–16.3 that will not survive a tighter machine. This extracts the arithmetic into a pure function and exercises the branch with synthetic arrays. **Reviewed at Full tier, correctness lens, single model family — independence NOT met.**
**The extraction was verified bit-identical by 20,163 differential cases** plus 20,000 median differentials — random, exact-binary-boundary and degenerate inputs (NaN, ±Infinity, −0, subnormals, MAX_VALUE), compared with `Object.is` across `resolves`, `lineMs`, `ceilingMs`, `controlStallMs`, the full reason string and the assertion bound. Zero mismatches. The sweep's threshold is **derived** from `STALL_BOUND / RESOLUTION_FACTOR`, verified by retuning four constants four ways, so retuning one retunes the expectation instead of leaving a test that pins the old design; its non-vacuity guard was proven to fire by pushing the threshold outside the swept range in both directions.
**One deliberate scope exception, recorded as one:** an empty sample now throws. `Math.max(...[])` is `-Infinity`, so `lineMs < -Infinity × 2` is false and **the gate opens with a real-looking 300 ms line, having measured nothing** — verified against the base implementation. Splitting that out would have meant knowingly merging a trust gate that asserts over an empty measurement. The builder later corrected its own framing: only the *floor* case is silent; an empty *control* makes `lineMs` NaN and bun fails loudly. Both are bugs, and *"a comment that overstates its own danger is a smaller version of the same problem."*
**Four mutations survived the first matrix, three worth closing, and F2 is the phase's thesis in miniature.** The PR argued at length — in the comment and in its report to me — that `!(lineMs < …)` was chosen over `>=` *because they differ on NaN*, and then pinned it with nothing: **the argument was the evidence.** F1: deleting `median`'s numeric comparator survived, because all six samples were comparator-blind (`median([2,10])` is 10 numerically, 2 lexicographically). F3: three independent `toContain` calls let the reason string's roles be swapped, so it would read "must stay under 200ms … cost as much as 300ms" inverted and a reader would tune the wrong knob — worse *because* this PR removed the inline copy and made that test the sole guardian. Eight mutations now, each finding caught by exactly its own test.
Merged on the existing PASS: the additions are tests, each demonstrated by its own mutation, plus one call-site substitution (`gate.controlStallMs` for a recomputed median) the lens had already verified identical across all 20,163 cases — no change to the mechanism it read. Also corrected: the PR body's "502 of the 400 swept points" was self-contradictory on its face; the real count is 100, and 502 was bun's diff *line* count. Author-declared verdict on a reviewer PASS — #24.
