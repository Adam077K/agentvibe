---
title: "W3-instruments — buffer ceiling, refusal diagnosis, judge-dir reclamation, and the tests that were missing"
date: 2026-08-29
engine: builder
task: w3-instruments
branch: feat/w3-instruments
base: 4ddc5c6a22c799f7b868f26545c507b03719944b
tier: full
qa_verdict: PASS
round: "fix round after evidence FAIL on E-1 plus two converged blocking findings"
verification: "VERIFIED-BY-EXECUTION throughout. Composite baseline test:merge-gate 126 pass / 0 fail before, 142 pass / 0 fail after (merge-gate 61 -> 67, produce-verdict 65 -> 75). MUTATION TABLE, 15 cells against that green baseline, each restored by cp from a git show snapshot and byte-identity asserted at the end: 14 RED under mutation, 1 GREEN must-not-fire (an inert comment reword). Cells: ceiling never applied - malformed never refused - ENOBUFS branch deleted - run-refusal relabelled - readCommitted bare catch - keep-flag ignored - operator dir reclaimed - sweep no-op - never registered - stderr not captured - exit handler removed - signal handlers re-added - ephemeral predicate INVERTED - caller env not threaded. Every mutation asserts the file differs from its snapshot before the suite runs, so a denied write cannot read as a passing cell."
finding_my_own_tests_were_vacuous: "THE MUTATION TABLE CAUGHT THREE OF MY OWN CELLS. J-7 measured a before/after listener DELTA around an arming that installs nothing, because judgeDirCleanupArmed is already true by then - so re-adding the signal handlers was invisible; and its 'exit >= 1' was satisfied by the test file's own cleanup listener, so deleting the library's was invisible too. J-9 drove a REFUSED outcome, where the tree is deliberately KEPT either way, so the inverted predicate and the correct one were byte-indistinguishable - reclamation only happens on an outcome that reached an answer, and the cell now sits there with a mirror arm. Mutation 04 never ran at all: its anchor matched 2 sites and the driver skipped it rather than mutating the wrong one. Two wrong-arm controls and one unreachable baseline, all three in the tests written to close a finding about untested code."
widening_enumeration: "The no-relabel predicate WIDENED from e.config (malformed env only) to e.aboutThisRun (malformed env OR a buffer limit). What the old predicate relabelled and the new one does not: an ENOBUFS refusal from rev-parse inside mergeBase's try, and one from git show inside readCommitted. What is still relabelled, unchanged and pinned by W-5: a base that genuinely does not resolve, which still reports 'cannot resolve origin/main - Fetch it first'. What needed the old behaviour: nothing - it produced a false cause, and a sweep over scripts/, .github/ and mission-control/ finds no consumer matching that string (positive control on the same sweep found 'no readable JSON' in produce-verdict.mjs; negative control returned empty)."
disclosure_acknowledged: "The captured stderr can carry the absolute repository path into a machine-readable field an operator may paste into a committed --evidence string. Capped at 400 bytes, cannot carry diff bytes or environment values. Acknowledged in the source beside the capture and judged acceptable against losing the cause of every refusal."
finding_panel_defect_env_not_threaded: "FOUND BY THE REVIEW PANEL, NOT BY ME, and it is a real defect I shipped in this round. produceVerdict read o.env to decide whether QA_KEEP_JUDGE_DIR was LEGAL while the arming site read process.env to decide what to DO, so a caller passing {env:{QA_KEEP_JUDGE_DIR:'1'}} had its request validated and then silently dropped - the tree it asked to keep was deleted. Measured before the fix: keep-requested and control both false, opposite instructions with byte-identical outcomes. Fixed by threading env through runProduceVerdict to armJudgeDirCleanup; J-10 pins it end to end through the public entry point with two controls (unset reclaims, explicit off reclaims) and goes RED when the threading is reverted - verified, exit 1, J-10 named as the failing cell, restore byte-identical. The panel's items 1 and 2 were one defect seen twice, as it said. Its item 3 (cleanup never driven end to end through the public entry) was already addressed by the re-aimed J-9 landed after the reviewed sha."
finding_stray_verdict_commit: "CORRECTED. My first write of this said the author line `Claude Code <test-engineer@evalove.local>` on 67c3e80 evidenced a foreign session. THAT MECHANISM WAS WRONG and I verified the correction myself: it is this repository's own configured user.email, 570 commits on origin/main carry it against 42 for noreply@anthropic.com, so it is the DOMINANT identity and mine is the unusual one. The author line evidences HOW the commit was made (a plain shell git commit taking repo config) and not WHO made it. A real observation with a wrong mechanism attached; the mechanism is the half that travels, so it is struck rather than softened. WHAT REMAINS ESTABLISHED: commit 67c3e80 `qa(verdict): PASS`, parent 634a0e7 which was my head, committed 23:12:30Z inside the 23:02:44-23:18:11Z window when my first mutation table was running, adding one .qa record `by: produce-verdict` whose subject is superseded and therefore inert. My branch adds FOUR .qa records, not the three I reported; three are mine. NOT MINE, ESTABLISHED BY EXECUTION: with a stub `claude` first on PATH and a positive control that fired, the current suite ran 75 pass with ZERO launches, the suite exactly as it stood at 634a0e7 ran 74 pass with ZERO launches, and that suite under mutation 06 -- the only mutation that changes launch reachability -- exited 1 with ZERO launches. NOT DETERMINABLE FROM HERE: what did invoke the producer. No top-level transcript was touched in the window and no transcript in 01:55-02:20 local records a produce-verdict.mjs invocation, against a positive control of 283 mentions in my own session transcript."
finding_q2_ANSWERED_a_real_session_ran_and_my_own_suite_spawned_it: "SUPERSEDES the UNKNOWN this file carried, and supersedes the narrower wording approved for it -- the question stopped being open. THE CLINCHER IS A SUBJECT MATCH: the stray record 67c3e80 binds subject 296001045698df6394c07ab5ab06d643111ac27ed93aed8038046845cb17c563, and `verdict.mjs subject --ref 634a0e7` returns THAT EXACT SUBJECT. 634a0e7 is my own commit at 02:02:44 local and the tree my first mutation table ran against; the record was committed at 02:12:30, inside that window, and my next commit is 02:18:11. At 634a0e7 `verdict.mjs check` returns ok:false reason:absent, so NO verdict bound that tree -- which means produceVerdict's cost short-circuit COULD NOT FIRE and the path continued to the launcher. The suite had seven call sites passing no launch guard. An independent reviewer drove that same path deliberately and observed a 235-second spawnSync(['claude','--print',<gate goal>]) leaving a 16 MB judge tree. So a real model session was spawned, materialised a judging project, reviewed a tree and recorded a binding verdict -- BY MY OWN TEST SUITE, accidentally. WIDTH: this is convergent evidence, not a direct observation. I did not watch the spawn in that window and cannot: no transcript exists for it and my own reclamation deleted the tree on the PRODUCED outcome. What is measured is the subject match, the absent verdict, the timing, and the mechanism demonstrated separately. The `record.by` caveat still stands and still matters: the field is written and NEVER VALIDATED -- a verdict here is hash-bound, not signed -- so a replayed flag is byte-identical, and it is the subject match rather than the `by` field that carries this."
finding_my_zero_launch_measurement_was_state_dependent: "I reported three arms, zero launches, positive control firing -- and a reviewer measured a real 235-second launch under the SAME mutation. Both are correct and mine does not generalise. THE MECHANISM: produceVerdict short-circuits on a binding verdict BEFORE reaching the launcher. My probes ran at b57e914, where `check` returns ok:true, so the short-circuit fired and no launch was possible. The first mutation table ran at 634a0e7, where it returns ok:false absent, so it was not. THE GUARD THAT PREVENTED MY LAUNCH WAS A VERDICT EXISTING, NOT THE CODE REFUSING -- and nothing in my command showed the state it depended on. A fifth member of the wrong-subject family: not the wrong tree, range, branch or suite, but the wrong REPOSITORY STATE, with the arms and controls all correct. My arm C compounded it by running the test file as it stood at 634a0e7, which had no J-10 and an older J-9, so it could not have exercised the sites the reviewer named even had the state allowed it."
finding_launch_reachability_is_now_PINNED: "J-13, one cell, added at the lead's direction. It asserts the measured table -- PRODUCED and BLOCKED do NOT reach the launcher, REFUSED DOES -- and it exists because THE GUARDS PROTECT THE SUITE AND NOT THE PRODUCT. A change widening the short-circuit set would spend ~40 minutes and ~3M tokens on a diff already blocked, and every cell in this file would stay green PRECISELY BECAUSE they are now guarded. Mutation-verified in BOTH directions, which a single direction would have missed: widening the set to include REFUSED -> RED; narrowing it to drop BLOCKED -> RED; an inert comment reword -> GREEN; both subjects restored byte-identical. The third assertion is a MUST-FIRE arm, not a nicety: without it the two zeros are satisfied by a build that never launches at all, which is the shape of every vacuous cell this round found in its own tests. `REFUSED -> run the gate` is what the producer is FOR and must not be 'fixed'."
finding_my_stub_control_was_on_the_wrong_arm: "Before accepting the environment explanation for the launch contradiction I eliminated my own instrument, and I should have controlled it the first time. My positive control proved THE SHELL resolved the stub; the arm that could fail silently was `spawnSync` INSIDE the node --test child, which I never controlled. Measured now: stub on PATH -> the stub answers from inside a test child, 1 log line; stub off PATH -> the real claude 2.1.246, 0 lines. The instrument was sound, so my zero was true for its state and the environment explanation is EVIDENCE rather than a convenient landing place. Had it been unsound, that explanation would have been available, plausible and wrong."
finding_the_rule_I_first_stated_would_have_mispredicted: "I told the lead the mechanism was 'a binding verdict exists'. The precise predicate is 'the pre-check returned PRODUCED or BLOCKED' -- nearly the same sentence, not the same rule. An UNREADABLE verdict is REFUSED and launches; a BLOCKED one binds and does not. My first version would have predicted the reviewer's detached clone was safe, and it was not. Both observed launches -- mine at 634a0e7 with no bound verdict, the reviewer's in a clone that could not resolve origin/main -- are ONE event under the corrected rule. A rule that explains the case in front of you is not yet one that predicts the next."
finding_the_class_was_seven_not_six: "Review named six unguarded call sites; the mechanical sweep found SEVEN of 38. The seventh predates these rounds -- a CONTROL arm of an existing test at line 788. Fixed by inlining a guard at all seven AND by J-11, which parses this test file's own source and fails on any call site lacking one, so site 39 cannot escape. Reported as a count and not as an act: 38 sites, 7 unguarded before, 0 after, verified by the same parser that found them, and J-11 goes RED when a single guard is removed."
finding_my_cleanup_destroys_the_evidence_of_a_gate_run: "A CONSEQUENCE OF MY OWN CHANGE, and it bears directly on the measurement being considered. produceVerdict now reclaims the judge project on every outcome EXCEPT REFUSED -- so a refusal keeps its tree and a SUCCESS deletes it. Success is precisely the case worth measuring for `does the runtime accept a materialised tree as a project`. Anyone running the producer to answer that must set QA_KEEP_JUDGE_DIR=1 BEFORE the run; afterwards the tree is gone. The keep-on-REFUSED rule came from review and is right for diagnosing failures, but its mirror is that the successful path is now the unobservable one. Flagged for a decision rather than silently reversed."
not_done: "The CLI signal-handler runtime comparison (405ms vs 20.9s) was NOT reproduced by me -- doing so safely needs the CLI driven into a long-running state, which is the very thing that spawns a gate session. I took the reviewer's measurement and removed the block; the structural facts are re-derived here (0 process.on(SIG in the module, control 3 process.on( of any kind). run-gate.mjs still sets no maxBuffer. Left deliberately at the reviewer's instruction: its exec sites are git diff --name-only and rev-parse, so output is bounded by path count, not content size."
---

Fix round on #126. Three blocking findings, all reproduced before being changed.

1. **E-1 — nothing asserted a line of this change.** 15 cells added across the two files
   `test:merge-gate` already runs, so no new step name. The specification is the mutation table.
2. **The ENOBUFS refusal was still relabelled** — its own thesis, unfixed. The marker covered
   malformed input while the comment claimed the class; ceilings of 1, 10, 40 and " 12 ", all
   well-formed, reported `cannot resolve "origin/main"` on a repo where it resolves.
3. **`readCommitted` asserted absence it had not checked.** A 20,301-byte record with a 105-byte
   diff read `ok:true` at the default ceiling and `absent` at 5000 — on the blocking path.

Also: signal handlers removed from the library and given to the CLI that owns the process;
`QA_KEEP_JUDGE_DIR` given a declared vocabulary so `0`/`false`/`no`/`off` stop meaning KEEP; and a
REFUSED run now keeps the tree it names, at one structural exit point rather than six return sites.
