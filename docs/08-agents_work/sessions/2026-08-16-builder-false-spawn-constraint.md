---
date: 2026-08-16
role: builder
task: false-spawn-constraint
tier: lite
qa_verdict: PASS
---

Deleted the three LIVE assertions of *"subagents cannot spawn subagents (nested Task is blocked)"* — `.claude/entry/ceo.md:11`, its byte-identical twin at `war-room/bin/PROJECT_NAME.tmpl:63` (the one that propagates: `bin/install-war-room.sh:72` substitutes it into every newly installed project), and the same premise restated as the *reason* for the design Workflow at `.claude/workflows/design-screen.md:53`. `git grep "subagents cannot spawn"` now returns **only historical records** — audits that quote the line in order to refute it (`AGENT-ARCHITECTURE.md:386`, `ORCHESTRATION.md:362`, `DECISIONS.md:548`, `adr/001:33`, and the measurement itself at `ENFORCEMENT-DIAGNOSTIC.md:109`). Deleting those would erase the record of the correction, so they stay.
Replaced the constraint **and** the dispatch-packet workaround it justified, rather than only deleting: nesting is measured to depth 2 (2026-08-11) with 49 depth-2 spawns and one depth-3 chain in the corpus; depth 1 is still preferred, and fan-out > 3 plus the QA gate go through a committed `.claude/workflows/` script, which is itself depth 1 and the only surface enforcing a return `schema:` (`AGENT-ARCHITECTURE.md:409-425`). The one open caveat is preserved **as an open question, not a prohibition**: the probe ran in plan mode with a read-only child, so write-capable nesting outside plan mode still wants one confirming test. Added dispatch-by-reference — file paths, not pasted bodies, above ~8,000 chars — against the measured tail (p99 212,282; max 1,069,297; `TOKEN-EFFICIENCY.md` §2.4).
Also corrected in the entry prompt, which is re-read every turn: the 9-chief/14-worker roster (10 names resolved nowhere) → the seven real engines; `TeamCreate`/`SendMessage`/`TeamDelete` → removed (`git grep` in `.claude/entry/` is now empty); `.claude/agents/ceo.md` → `orchestrator.md` (the former is a 23-line shim); and the ordered `MANIFEST.json` read (~15k tokens, forbidden by `CLAUDE.md:47`) → the two-tier router at `.claude/skills/routers/INDEX.md`. Net **3,126 → 2,975 bytes**, inside the 4,096 budget pending founder decision #56.
**`tier: lite`, not the `trivial` the brief specified** — `node scripts/classify.mjs` computes `floor=lite` because `war-room/bin/PROJECT_NAME.tmpl` matches no rule and takes the default. I recorded what the classifier says rather than what the brief assumed; that is the whole point of this lane.
**`npm run check` exits 0 on this branch**, all 20 steps. One caveat for whoever runs it next: an earlier run on the untouched baseline failed at `check:mc` on `mission-control/test/live.test.ts:375`, which asserts a cold and a warm read of the live transcript corpus see the same session count (expected 2585, received 2586) — concurrent sessions write transcripts between the two calls, so that assertion is racy on a machine with agents running, and a failure there is not evidence about the diff.
