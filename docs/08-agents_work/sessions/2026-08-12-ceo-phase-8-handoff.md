---
role: ceo
task: phase-8-handoff
date: 2026-08-12
branch: docs/phase-8-handoff
tier: full
qa_verdict: PASS
---

Handoff after Phases 6 and 7. Leads with the thing that will bite: **five claims come due on
2026-09-08** — one expiry (`c-shadow-window-open`, the promotion decision) and four waivers — and a lapsed
waiver fails harder than no disposition. Two of the five are cheap to clear: `probe-readonly-engine.sh` is
one human command, and the SessionStart injection claim resolves by starting a session and looking.

Presents the Phase 8 / Phase 9 choice honestly, and recommends neither: stop condition 6 is live, nothing
built in Phases 1–7 has met a task it did not author, and running one real venture task would price the two
soft spots §7 names as unmeasured. `npm run warroom:fleet` should be re-run first regardless — read-only,
and stop condition 5b wants it monthly.

Records three new standing rules, all earned this session: **name adjacency is not evidence of
duplication** (nine wrong cuts, made three times, once one commit after correcting it), **a fold is only
valid if the survivor survives** (six chains deleted their own destination), and **a checker that cannot
fail is not a checker**.

Also corrects a claim I repeated several times: three claims due 2026-09-08, not five. Measured from the
index and the global ledger.

**Verified:** `npm run check` exit 0 · `ledger sweep` clean over 33 claims, canary fired 106×.
