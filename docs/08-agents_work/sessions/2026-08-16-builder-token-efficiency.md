---
date: 2026-08-16
role: builder
task: token-efficiency
tier: full
qa_verdict: PASS
---

**DECISIONS.md grew to 58,166 bytes (38 entries) while the stated cap was 50 entries.** The entry cap was under budget; the byte budget was not, because CLAUDE.md declared the entry cap only — no byte budget existed, so nothing could measure a violation. TOKEN-EFFICIENCY.md §3.2 measured it at 46,655 bytes as of the previous audit and already marked it VERIFIED as exceeding budget. By 2026-08-16 it had grown 25% further with no mechanism to notice.
**Archived 15 entries** (Phase 1-3 specifics, superseded decisions, and entries whose information lives in other documents) to `.claude/memory/DECISIONS_ARCHIVE.md`. Nothing deleted. Remaining: 23 entries, 39,909 bytes — under the 40,000-byte ceiling.
**`scripts/check-memory-budget.mjs` enforces both the entry cap (≤ 50 dated `## YYYY-MM-DD` headings) and a byte ceiling (≤ 40,000 bytes) on DECISIONS.md, plus the ≤ 100-line cap on LONG-TERM.md.** The byte cap is derived from the entry cap: 50 entries × 800 bytes/entry = 40,000 bytes. At the pre-trim 38-entry average (1,530 bytes/entry) that cap would have been hit at 26 entries — the cap is intentionally tighter than entry × average to penalise bloated entries, not just count. The checker counts entries by the `/^## \d{4}-\d{2}-\d{2}/m` pattern, so the `## Format` heading in the template section is excluded. 8/8 mutation tests pass, including one asserting the format heading does not add to the count and one pinning the real repo against the floor.
**`scripts/check-dispatch-prompt-size.mjs` checks the SOURCE TEXT length of the first argument to each `agent()` call in `.claude/workflows/*.js`.** TOKEN-EFFICIENCY.md §7.3 (VERIFIED on 2,412 measured dispatches): p99 prompt = 212,282 chars, caused by verbatim peer-output paste. Convention: above 8,000 chars, pass a path not a body. The current 4 workflow files have 12 dispatch sites; every inline literal is under 8,000 chars (largest: 817 chars, research.js:131 — a variable, not an inline literal). The check passes today and guards against future violations. Non-vacuity floor: 12 sites asserted. 8/8 tests pass, including a mutation asserting the script's own source file is not in the scan surface.
**Coverage limits, stated:** The dispatch-prompt check measures source text only; a template `\`hello ${bigVar}\`` that is 30 chars in source but 500,000 chars at runtime is out of reach. Runtime-large prompts caused by dynamic content pasting are not caught — stated in both the script header and PR body. The memory-budget check cannot detect whether entry content is well-summarised or redundant.
**CI line needed but not added (owner: lane editing ci.yml):** `npm run check:dispatch-prompt && npm run check:memory` after the existing `check:dispatch` step, or merged into the `check` target (done here via package.json). The exact line in `.github/workflows/ci.yml` to add: after the `node scripts/check-dispatch-agenttype.mjs` invocation, add `node scripts/check-dispatch-prompt-size.mjs` and `node scripts/check-memory-budget.mjs` as separate `run:` steps, or rely on the updated `npm run check` chain.
**Verified:** `npm run check:memory` 8/8 pass; `npm run check:dispatch-prompt` 8/8 pass; `node scripts/classify.mjs scripts/check-memory-budget.mjs scripts/check-dispatch-prompt-size.mjs` → `floor=full`; `node scripts/ledger.mjs verify` → 7 would_block (shadow) · 0 block (baseline origin/main was 9 — reduced by this PR). `npm run check` exits 0.
