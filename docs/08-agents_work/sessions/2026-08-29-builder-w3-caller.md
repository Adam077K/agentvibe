---
role: builder
task: w3-caller
tier: full
qa_verdict: PASS
date: 2026-08-29
base: 4ddc5c6a22c799f7b868f26545c507b03719944b
---
- Wired `consume-dispatch.ts` to `scripts/produce-verdict.mjs`, which shipped in #125 and was invoked by nothing. Denominator: **diffs that need the gate and have no binding verdict**, not dispatches. The producer runs after the launch returns, because the subject hashes the diff.
- `classifyVerdictProduction` takes the state from the producer's `--json` payload and requires the exit code to AGREE; signal, timeout, unparseable payload, unknown outcome and exit 64 are `unresolved`. No state comes from an exit code alone; no `passed` member.
- The symbolic-ref item was misattributed to `deriveGateReachability` (holds no ref). Fixed at its real site, `routeGate`, via `refTip` from the router's `verdictRef`. A1: 5 entries bought 5 panels; deduped by subject per run. E1: `describeRef` was reached 14x with nothing asserting its output.
- **Q1 collapse**: `refTip` is `string`, never null; `refTipReason` deleted as a FIELD; `describeRef` single-branch; the `<unpinned>` key form gone. **`refTipReason` SURVIVES AS A LOCAL IDENTIFIER** at `consume-dispatch.ts:359-362`, where it names the string relayed into the refusal — requirement 2 being implemented. A grep on the bare name returns 7 lines, **3 of them comments explaining the removal**, so a reader doing exactly what a careful reader does concludes the opposite of the truth from evidence that is about the truth. The field count is 0; `expect(r).not.toHaveProperty('refTipReason')` pins that half.
- **Q4**: the first terminal line carries an explicit `unresolved` instead of silence, closing an ambiguity the reorder itself introduced.
- **THE ENUMERATION THE COLLAPSE WAS AUTHORISED ON HAD A FALSE ROW, corrected in this round.** It claimed a tip `verdict.mjs` cannot resolve "cannot carry a binding verdict, so nothing that can succeed needed it". `produce-verdict.mjs` mentions `verdictRef` on ONE line — to say it deliberately does not read it (control: 35 lines for `invocation`) — and is spawned with no ref, re-deriving from its own router call. The refusal is KEPT on its true reason: without a pinned tip nothing can tell one diff from another, which is the collapse the delta found. **Named residual: for a foreign router that cannot pin `verdictRef` but emits a sound `invocation.args.ref`, we skip a panel that could have run — 1 before, 0 after, now pinned by an observed run count in a fixture that HAS a producer.**
- Other corrections this round: a superseded doc block describing the deleted cache was still present above its replacement (`THE UNIT OF SPEND` 2 -> 1); "no per-root collapse" was true of the body and false of the composition, so it now names what guarantees the `''` sentinel arm is unreached (measured 150/0 under a throwing IIFE); and the cache-deletion rationale and the tip-fallback rationale could not both stand unqualified — the fallback is sound only while `origin/main` is fixed for the run, and the mid-run base move is named as a residual in the skipping direction.
- Delta fixes: F1 the skip record said three things it could not know; F2 fixtures never wrote `scripts/verdict.mjs` so no test reached the subject branch; F3 two assertions survived mutation; F4 the new spawn was unbounded; F5 half of A3 is inert and says so; F6 two comments described readers that do not exist. The "interrupted run can pay twice" comment was false and is deleted.
- VERIFIED-BY-EXECUTION: `dispatch.test.ts` 91 (baseline) -> 150 pass, 0 fail, 604 expect() calls; `tsc --noEmit` exit 0; mutations run each round with a must-not-fire control and byte-identical restores. `crosscheck` 18/0, `index-cache` 32/0, `units` 36/0, `write-barrier` 7/0 individually; combined 92/1 on the ledger crosscheck's documented 120s budget (57,293ms per verify, run twice, 4.5% margin) — pre-existing, touches no file in this diff, NOT greened by raising it.
- NAMED RESIDUALS: `maxTurns` truncation (routed to `NO_RETURN`); `routeGate`'s unbounded spawn (pre-existing); `origin/main` moving mid-run; the A2 regex refuses a 64-hex tip; the skipped panel for a foreign router with an unpinnable tip.
- Standing caveat: author-recorded, deterministic floor, one agent, one model family. 2-of-3 multi-judge across >=2 model families NOT met. Accepted risk, exit 2026-11-17.
