---
role: builder
task: prompt-standard-lint
date: 2026-08-16
branch: feat/prompt-standard-lint
tier: irreversible
qa_verdict: PASS
---

PROMPT-STANDARD.md §6 is now `PS-*` rules inside `schema-lint.js` — 14 new blocking rules and 5 warnings, each asserted in `scripts/prompt-standard.test.mjs` to hit **zero on the seven live engines** and to **fire on a constructed violation**, which is §6.4's whole method. 59 tests, all pass. `PS-BODY-VAGUE` reproduces §0's measurement exactly (6 of 7 files, 10 sites) and is why it can only warn.
**Two real bugs fixed.** `maxTurns` was type-guarded with `typeof === 'number'` while `parseFrontmatter` coerces only `/^-?\d+$/`, so `maxTurns: "30"` skipped the range check *entirely* — a guard meant to bound an error silently disabled it. The header cited `07b-AGENT-TEMPLATE.md`, which exists nowhere in this repo or its history. The `[5,30]` cap becomes `[5,120]`: at 30 it set the value rather than bounded an error, and a measured reviewer run needed 68 tool calls. No engine's declared value changed — but the ceiling string is hard-coded in `c-maxturns-binds-when-agenttype-named` (#67), so that claim and CLAUDE.md's Project State were reconciled in this branch.
**`VALID_MODELS` is the Claude-5 set** (TOKEN-EFFICIENCY.md §6, the only VERIFIED inventory), so all 7 engines move in the same PR: orchestrator/designer/reviewer/reviewer-readonly `claude-opus-5` xhigh · builder/sourcer `claude-opus-5` **high** · framer `claude-sonnet-5` high. Sources: CONTROL-PLANE §2.1/§3.1, PRODUCERS §3.1/§4.1, GRANT-HOLDERS §3.1. **Two are derived, not specified:** `reviewer-readonly` has no spec section (taken from `reviewer`, which its own file says it matches), and `framer` has none at all because ROSTER-SIZE §7.6 cuts it — the founder kept it today, so it holds the nearest live model to the pin it had. Both want a founder decision.
**`effort` is required and enum-checked, and nothing here implies it binds.** The VALUE binds where set; whether the FRONTMATTER FIELD is read is unverified. Recorded as `c-effort-frontmatter-binding-unverified` — **`verified_by: command`, not `judge`**: a judge claim with `judged_by: []` and `risk: high` resolves `unresolved` forever, since the ≥2-model-family predicate cannot be satisfied in-process, and two such claims already sit permanently unresolved here. The command asserts the checkable half — every non-shim agent declares a valid `effort`, and schema-lint's "NOT VERIFIED" note survives — and it fails on both constructed violations. `checkEngineRoster()` now fails on ENGINES-vs-disk drift in both directions.
**Verification:** `schema-lint` **18 pass · 0 fail · 8 warnings**, exit 0 (7 engines + 11 shims; was 18/0/1 — the 7 added are PS-BODY-VAGUE ×6 and PS-SECTION-ORDER ×1, both advisory by design). `ledger lint` clean, `build --check` matches at 38 claims, `verify` **79 pass · 5 would_block · 0 block** with both claims above passing — the 5 are pre-existing. **`npm run check` exit code is reported separately in the return; `check:mc` fails identically on a clean `origin/main` tree** (`crosscheck.test.ts` times out at 120s against a 3.07GB corpus) and mission-control is another lane's. **Not covered:** no agent body was rewritten (separate PR); `PS-DISPATCH-BRIEF-SIZE` is unimplemented — it reads `.claude/workflows/*.js`, which this lane does not own.
