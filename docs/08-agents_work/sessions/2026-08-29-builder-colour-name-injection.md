---
date: 2026-08-29
role: builder
task: colour-name-injection
qa_verdict: PASS
tier: full
risk: full
branch: integration/design-layer
commits: 2
---
# One field into the sink was guarded and its sibling was not

**THE P1 IS CLOSED AND THE ASYMMETRY IS THE FINDING, not either payload.** `validateSeeds` ran every colour VALUE through `hexToRgb` and never looked at a colour KEY, while the key reaches **four** sinks in four languages: a CSS custom-property ident inside `@theme` (`renderCss`), a single-quoted TypeScript string key (`renderTs`), a JavaScript object key (`renderJson`) and a markdown table cell (`renderContrastMd`). Both reported payloads reproduced verbatim at exit 0 before the fix — `a: b; } :root{ --color-text:#ff0000 } @media all { --z` closed `@theme`, and `x': String(globalThis.__PWNED = 'code-executed') //` set `globalThis.__PWNED` when the generated `tokens.ts` object literal was **evaluated**, not read. Both now exit **2**, read from `$?` directly. `PALETTE_NAME` is `FAMILY_MEMBER`'s unquoted branch minus the space: a colour name has no quoted form at any of its four sinks, so the deny-list-inside-quotes shape the family guard needed has nothing to attach to here.
**NON-ASCII LETTERS ARE ADMITTED DELIBERATELY, because this file has already made the opposite decision once and it was wrong.** CSS Syntax §4.2 admits any non-ASCII code point in an ident; `--color-ミッド` reaches all four files intact and `@theme` still closes on the last line. The grammar is still narrower than CSS: U+00A0, U+200D and every C0/C1 control are refused. **The must-accept list was derived from the repo, not from the brief** — `paletteNames(seeds.color)` over the committed file, twelve names plus the `$comment` metadata key, and the `$`-prefix filter (which was spelled twice, in `validateSeeds` and in `buildModel`) is now spelled once so a key the validator skips cannot be a key a renderer emits.
**THREE P3s CLOSED AT THE SAME TRUST BOUNDARY.** (1) `__proto__` passes the grammar — it is letters and underscores — so it is refused **by name**, one guard with three clauses rather than a second mechanism; measured before the fix it was present in tokens.css and tokens.ts and **absent from tokens.json**, `Object.prototype` untouched, two artifacts disagreeing with `--check` calling both correct. (2) A `note` carrying `|` and a newline emitted **a fourteenth row for a pair nobody authored** reading `**21.000:1** · pass · pass`; refused as a deny-list, not an allow-list, because a note is prose and the committed thirteen carry `Δ`, `—` and parentheses. Colour names are **not** re-checked at that sink — the name grammar already refuses `|` — and a widening of it has to be re-checked there. (3) `type.{ui,display}.steps` is bounded at 64, ~4x the widest band in the corpus, derived on each call.
**THE `steps` CRASH IS NOT THE ONE REPORTED, AND THE DIFFERENCE IS THE ARGUMENT FOR A BOUND.** `steps: 1e9` and `1e10` both give **exit 134**, SIGABRT, *JavaScript heap out of memory* — not a `RangeError`, which V8 never reaches, and not exit 2. A signal death cannot be converted by the CLI's `catch`, so the exit-code contract at the top of the file was silently untrue for that input. Measured with `--max-old-space-size=200` so the measurement is fast rather than fatal.
**CREDENTIALS ARE STRIPPED AT BOTH PERSIST POINTS, AND THE UNCONDITIONAL VERSION WOULD HAVE BROKEN THE REPO.** `sourceRecord` was named in the review; `analyse` writes `url` and `finalUrl` into `measured.json`, which is committed too, so fixing only the named one leaves the identical string one function over. `stripCredentials` returns its input **unchanged** when there is nothing to strip: `new URL('https://linear.app').href` is `https://linear.app/`, all five committed `SOURCE.yml` files carry the bare form, and `writeReference` compares that field to detect a slug collision — mutation M14 confirms it, reddening the collision test and the SOURCE.yml test as well as the new one.
**EVERY FIX WAS MUTATION-TESTED AND ALL 14 MUTATIONS WERE CAUGHT.** Removing either guard call, the `__proto__` clause, the `steps` ceiling, either length cap, the `|` from `NOTE_FORBIDDEN`, `paletteNames` from `buildModel`, or either `stripCredentials` call reddens a named test; so does narrowing `PALETTE_NAME` to ASCII (the N1 regression shape, one field over) and dropping `\p{N}` or `\p{M}` alone. **No mutation survived, so no new test is vacuous.** The one control that had to be built through `JSON.parse` rather than assignment is `__proto__`: an assignment invokes the setter and creates no key, so a test written the obvious way would pass against a generator with no guard in it.
**Verified:** `npm run check` → **48 of 48 · 0 failed · exit 0** (201.9s, `$?` read directly, sandbox armed). `build-tokens.test.mjs` 43 → **51 tests**, `extract-reference.test.mjs` 53 → **54**, both files green together at 105/105. No new governed script name: both files already ride `test:lenses` and `test:merge-gate`. `scripts/lib/check-suite.js` and `.github/workflows/**` are **byte-identical to origin/main** (`git diff --quiet` → 0 for each).
**The standing caveat applies and this verdict does not discharge it.** `qa_verdict: PASS` is author-recorded against a deterministic floor, one agent, one model family; the reviewer who raised these findings has not re-reviewed the fixes. Two commits rather than four: the p1 and two of the p3s share one validator and one test file, so a per-finding split would have produced commits whose trees were never run.
