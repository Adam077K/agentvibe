---
date: 2026-08-14
role: ceo
task: mc-project-inbox
tier: full
qa_verdict: PASS
---

PR5 completes the six views: Project (drill-down from a Fleet row) and Inbox, `projectEmptyState` async **and bounded**, plus the two deferred craft items. **Full tier, three lenses, Claude-only — independence NOT met.** Correctness FAILed twice before clearing; security PASSed with two MEDIUMs; craft passed with seven. My own figure was wrong again and a worker caught it again: Beamix's probe is **107,806 ms**, not the 3,657 ms I briefed — I had timed `grep … >/dev/null 2>&1` without checking it ran to completion, the third such error this session. That correction changed the design: async alone cannot fix an unbounded recursive scan, so it is bounded at 10 s and says so, taking `GET /api/project/Beamix` from **113,158 ms to 28 ms**.
**One defect shape appeared three times and is now closed by name: a rendered fact pinned while its producer runs free.** PR4's C1, then the 10 s bound (`empty.ts` 124-134 executed **zero** times while a test named for it demonstrated a fact about *Node*), then the freshness stamp — where inverting one line restored #39 verbatim, "fetched · updated 3s ago" over a panel saying the ledger could not be read, with the full suite green. The fix each time was the same and needed no DOM: extract the decision into a pure function and assert **producer and consumer in one place**. The reviewer proved it is not a tautology by noting a tautological version would have passed all three mutations.
**Three defects were in guards, not in code.** A guard importing its threshold from the module it guards (`maxSeen <= PROJECT_PROBE_MAX_CONCURRENT` — raising the cap 2→64 kept the suite green while 20 greps ran). A concurrency sampler that never overlapped its subject, because `execFileSync` needs the same JS thread the semaphore hands its slot over on. And a non-vacuity guard aimed at the wrong premise — asserted on `excluded`, which did not need it, while `unreadable`, the whole point of the split, reduced to `2 === 2`.
**Coverage was the cheapest instrument by a wide margin**, and it disagreed with prose twice: a residue disclosed as "one line of JSX" measured **91 lines — the entire `App()` component**; and a 99.08% figure counts three unexecuted callback bodies as covered, because line coverage marks the **hook call**, not the body. I approved the first trade on a number I had not checked. The builder's own tell, worth more than any single fix: *"I wrote the producer and the consumer in the same PR, and only one of them appeared in a test file."*
**Logged, not fixed:** #41 (on `main`, `changedFilesFor`'s partial recovery truncates mid-line and puts a fabricated filename into the conflict map — fabrication reaching a rendered figure; needs both whole-line parsing **and** `readable:false` on rejection, because *the signal for "this is partial" is the rejection, never the buffer*), #43 (a PR4 pin flaking at 0.5% because its gate watches idle noise while the confound is the shared spawn floor — gate on it, never divide by it), #42, #44. Merged on reviewer PASS under the Founder's rule-8 widening for PR4/PR5 only; #24 stands.
