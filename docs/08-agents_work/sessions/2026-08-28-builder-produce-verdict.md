---
role: builder
task: produce-verdict — the step that makes a QA verdict exist
date: 2026-08-28
branch: feat/produce-verdict
base: b4ea862
tier: full
tier_driver: scripts/** (scripts/produce-verdict.mjs)
qa_verdict: PASS
---

- `scripts/produce-verdict.mjs` runs the panel in a bare session whose cwd is a materialised copy of `origin/main`, then decides only from a verdict record read with **main's** `verdict.mjs`. Four states: PRODUCED 0 · BLOCKED 1 · REFUSED 2 · NOT_REQUIRED 3 · usage 64.
- **F-2 (HIGH, fixed).** `tip` was frozen pre-launch and the post-check read there, but the session records *and commits*, putting the record on a descendant — so **PRODUCED was unreachable through a launch** and closed only on an undocumented second invocation. The post-check now reads at the post-launch HEAD and **requires the subject to equal the one reviewed**; the subject excludes `.qa/verdicts/*.json`, so equality proves the session added a verdict and nothing else. The pin is kept for the pre-check, where it is right.
- **F-1 (HIGH, fixed).** `run-gate.mjs` reads no repo flag (grepped: 0, controls 4 and 5) and resolves `REPO_ROOT` from its own location, so the CLI's `--repo` selected a tree nothing downstream honoured. The flag is **removed**; a programmatic `repo` that is not `harnessRoot` is REFUSED; and the human path now prints tree, ref and head.
- **F-3 (MEDIUM, fixed).** `verdict.mjs record` accepts only `PASS|FAIL`, so matching the literal `BLOCK` made BLOCKED unreachable and read a bound FAIL as "establishes nothing". `FAIL` and `BLOCK` both map to BLOCKED; `buildGoal` now asks for `PASS|FAIL` and says to record nothing on a refusal.
- **F-4 / F-5 (LOW, fixed):** `--help` exits **64**, outside the four terminal codes; a flag with a missing value is refused rather than defaulting — `--launcher --json` no longer spawns the real `claude`.
- **E6 (p1, fixed).** Nothing asserted the `test:merge-gate` argv names this file, so deleting the filename dropped its tests with everything green. `check-suite.test.mjs` now asserts **every** `scripts/*.test.mjs` is named by some script, with an exemption map that must say why and may not rot. **Found in passing:** `claim-append.test.mjs` is run by nothing — pre-existing, exempted with its reason, deliberately not wired by this lane.
- **The meta-finding is what drove the round:** every seam injected for testability was a seam the tests stopped watching. Four tests now drive the **real** `verdict.mjs` end to end — record, commit, re-check — and no longer stub the relationship.
- Evidence: `npm run check` **48 of 48 · 0 failed · exit 0**, 190.1s, sandbox armed · `produce-verdict` 40 pass · `check-suite` 72 pass · 8-cell mutation table, 7 FIRED and 1 SILENT, all as predicted; "post-check reads the pinned tip only" fires with 3 failures, so the fixtures express the defect rather than the fix.
- **F-1 demonstrated by execution, not argued:** the reviewer's exact arm — a fixture holding a committed FAIL at tier `full` — is now `--repo` → **exit 64, unknown flag** (the flag is gone); programmatically, `repo !== harnessRoot` → **REFUSED**. And the whole pipeline ran end to end on this tree against the **real** router, a real 957-blob materialisation and the **real** `verdict.mjs`: **PRODUCED**, bound to `25cf9d15…`, with the human path printing tree, ref, judge project and subject (grep for this tree → 1, was 0; negative control on another tree → 0). Everything but the panel launch.
- An unknown flag now exits **64**, not 2: `REFUSED` means *the gate ran and established nothing*, and a typo is not a gate run. Same argument as `--help`.
- **A-1 (HIGH, fixed as a CLASS not a site).** `--git-ref` selected the ref the judge is materialised from, unvalidated, so every guard in `materialiseJudgeProject` passed tautologically — completeness and identity are both measured *against the ref you name*. `--git-ref HEAD` → EXIT 0, PRODUCED, launched=false. The judge ref is now the constant `JUDGE_REF`, removed from the CLI **and** from `produceVerdict`'s parameters. `--repo` had been deleted the round before for the weaker version of the same thing, so the fix is a registry: **`FLAG_ROLES` gives every flag a role and `selects-what-is-measured` is forbidden**, failing on any flag with no role. Sweep result: 6 flags, 0 forbidden; programmatic params carry no `gitRef`. The one residual is stated — `harnessRoot` still selects the router, which is PR-provenance by construction and cross-checked by `crossCheckArgs`.
- **A-2 (HIGH, fixed).** The subject-equality claim was false in the one class that matters: `verdict.mjs` excludes `.qa/verdicts/*.json` from every subject, so a session could record its verdict **and also commit forged PASS records for subjects it never reviewed** with the subject byte-identical. The session may now commit **exactly one path, whose basename is the subject reviewed**. The equality is kept as a second instrument over the same property, reachable only through the `readVerdictArtifact` seam — an untestable guard is one someone deletes. *Not fixed and not mine: an author who controls a future diff can plant a record for it. That is the recorded "hash-bound, not signed".*
- **B-1 / B-2 / B-3 (fixed):** the four exit codes are pinned **by value** (a BLOCKED/REFUSED *swap* satisfied the old shape assertions); `ok:true` at a non-zero exit is REFUSED — Rule 10, the checker said it failed; an operator `--judge-dir` is canonicalised like the default.
- Round-5 evidence: `npm run check` **48 of 48 · 0 failed**, 155.1s · produce-verdict **48 pass** · 10-cell mutation table, 9 FIRED and 1 SILENT as predicted, restores verified byte-identical with `cmp`. **Two cells were invalid on first run and both were mine:** the basename bound was SILENT (only the *count* was tested — a single verdict file under the wrong subject would have passed), and the B-1 swap anchor never landed because a literal `\n` in argv matched nothing. Both closed.
- **A-3 (HIGH, fixed).** `crossCheckArgs` called itself distrustful and re-derived **two** of the invocation's **four** load-bearing values: `tree` and the ref's TIP. The ref's BASE and `tier` were unguarded, so a PR-shipped router emitting an honest tip and a dishonest base (`<S2>...<S2>`) handed the panel an **empty range** while the verdict bound a real diff — EXIT 0, PRODUCED. Now the separator must be `...` and the base must be `JUDGE_REF`, re-derived from the same `args.ref` the tip comes from; and `args.tier` must equal what the classifier computes over that range. **`decision.verdictRef` is deliberately NOT read** — consuming a field the router computes is the trust this function exists to withhold — and a test pins that as a choice rather than an oversight.
- **C-1 (p2, fixed).** The F-1 narrowing made every CLI test terminate before the pipeline (`cwd: <repo>/scripts`), so exit **0, 1 and 3 were never observed from a real process** — only asserted against the `EXIT` object. A fixture harness now holds the producer, a faithful router stub and the real checker, and all four codes are read off `process.exitCode`. The F-5 control was misdirected too (`status !== 64` was satisfied by the F-1 refusal) and now runs where the pipeline is reachable.
- **C-2 (p3, fixed):** an unreadable subject is REFUSED *as unreadable*, no longer as "the gate session moved the reviewed bytes" — the instrument's silence is not an accusation. **E-1 (p3, fixed):** the exemption floor was vacuously satisfiable (45 spaces passed); it is a named predicate now, with **one case per conjunct** so neither can be deleted silently.
- Round-6 evidence: `npm run check` **48 of 48 · 0 failed** · produce-verdict **57 pass** · check-suite **73 pass** · mutation cells all as predicted after two were corrected. **Both corrections were mine and both were the same error:** C-2 and E-1 were fixed in the code with no cell that could fail — and E-1's first cell was *still* invalid, because whitespace is caught by the word count, not the density check, so the two conjuncts needed a case each.
- **A-3 closed as a CLASS, and writing the registry found two more.** `INVOCATION_TREATMENT` declares every field the invocation carries with what happens to it; `trusted` is forbidden and a field with no entry fails, checked against what a real router actually emits. It is `FLAG_ROLES` pointed at the other input — flags are what the *operator* supplies, these are what the *router* asserts. The two it exposed: **`scriptPath` was taken from the invocation**, so the router chose which workflow the judge ran (main's files only, but "which gate runs" is not its call) — now re-derived from `QA_SCRIPT`; and **`args` was forwarded whole**, so a fourth key reached `qa.js` verbatim through the goal — now refused at the key. `gates.yml`'s UNMODIFIED is honoured: values are not rewritten; unknown keys are not accepted.
- **Unresolved:** the end-to-end panel has never been driven through this mechanism, and nothing in CI invokes the producer — only its tests run. Author-recorded, one agent, one model family: *the checks ran and are green* is not *the tier was satisfied*.
