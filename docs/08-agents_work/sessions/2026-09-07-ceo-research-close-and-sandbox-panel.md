---
date: 2026-09-07
role: ceo
task: research-close-and-sandbox-panel
qa_verdict: PASS
tier: trivial
branch: ceo-1-1788609834
pushed: false
---
Closed the research round. Three lanes (W/M/C) and the terms answer recorded verbatim in `final-v2/research/close-{W,M,C}.md` and `review/terms-row-1.md`; folded in `a065325`, `1411fbe`, `b10f2a1`; dispositions in `DECISIONS.md` §29.
Founder: Reading C on the terms — §I row 1 CLOSED as v108 `class: ratified`; "change nothing yet" on the sandbox, recorded with a trigger (revisit when `bin/run` exists) not a date; replicate before reporting; and v107, the output-shape row, from their `attention-span` direction.
The off-hours metric could not answer its own question — episodes give zero gaps ≥1 h, contiguous spans give 70.0 h and an 11.08 h longest, because DarkWake ends an episode every ~16 min. My "power cable not cloud lane" disposition is struck as withdrawn by its author; §I row 15 is OPEN again.
`/goal` never runs its condition: an exit-code arm was judged met on a transcript narrating `echo "0" >` while `test -f` never executed. v12 and O21 corrected, not annotated.
O71's concurrency ceiling 3 → 17, with `vm_stat` Swapouts as the instrument because free memory moves the wrong way across the knee. v90's Gemini route closed by Google 2026-06-18 — availability, not terms, and the founder's originated intent untouched.
COVERAGE closed for v83–v106 (`f87e943`, 147 rows, `?` 2 → 0) and named one founder item with no mechanism anywhere: voice output.
Five sealed sandbox lanes converged on per-child confinement; none proposed disarming; the founder deferred. Their sharpest finding is not about the sandbox — what binds a night child is argv alone, and that survives any posture.
Two ways a child runs unconfined while its settings read enabled (8 of 9 cells against 0 of 7 controls; and `filesystem.disabled` via `--settings`). `--restricted` refuses one, measured. Reproduction drafted, nothing sent.
Three CLAUDE.md claims falsified — `allowLocalBinding` exists and works, `enableWeakerNestedSandbox` is Linux-only, the worktree wall is the documented protected-paths list. Recorded, not edited: irreversible tier, another PR's.
Nothing built, installed, spent, published or pushed; no settings file on this machine modified. Single family throughout, so the 2-of-3 and two-family predicates are unmet on all of it.
