---
date: 2026-08-16
role: ceo
task: incremental-index
tier: full
qa_verdict: PASS
---

Mission Control's cold start no longer reads the whole corpus. `IndexStore` gains a counting seam and a persisted index; `refresh()` was already append-aware but unreachable cold because nothing survived the process. **`filesRead` 2,610 → 0. Transcript bytes 3,059 MB → 0**, replaced by 4 KB probes per transcript — 10.65 MB across 2,610 today, **a rate, not a constant**, and quoted as one after it was twice quoted as a fixed figure. Cache 4.39 MB. Wall clock is printed and **not asserted**.

**The design was chosen by measurement and the brief's premise was wrong.** `refresh()` already handled appends; the cheaper alternative the CEO proposed was already shipped. Churn between cold starts is **1.85% of bytes over 24 h**, pessimistic — live sampling showed 44 MB touched against 0.47 MB appended.

**Nine review rounds. The reviewer never returned a clean pass on a first look**, and every round found something real.

**The p1 was a design-level omission, not a coding error.** The first version persisted on **every** refresh while the session stream ticks at 1 s — **4.38 MB per tick, ~378 GB/day**, doubling the tick. The design costed the write **once**; frequency appeared nowhere in it. Fixed with a dirty check *then* a floor, in that order, because a floor alone still rewrites an unchanged index every five minutes forever. **378 GB/day → ≤1.26 GB/day, and zero while idle.** Cost measured both directions: ~30 ms aged-cache penalty against a ~2,500 ms cold build — and warm got **faster**, because a clean start is not dirty and skips the save.

**A security guard was re-scoped, and the ordering was the condition.** The behavioural tree-snapshot barrier landed **alone** in `c8dee7f` and was proven catching what the regex catches; the regex was re-scoped two commits later in `589196c`. The barrier caught a `fs.copyFileSync` the regex was **invisible** to, so the token list widened too. `openSync` is a **declared** blind spot — `index-store.ts` opens read-only with it, and a guard that cries wolf gets disabled.

**Three vacuous verifications, all self-caught, all in the verification rather than the thing verified.** A multi-line injection targeting an import line the file did not have — nothing inserted, green read as proof. A leftover sanity assertion firing before the injection was written — both runs "0 fails" against an **unmodified file**. And a timeout anchored on a non-unique shape that attached 120 s to an unrelated test 60 lines earlier, so **a fix that appeared applied, was not, and left the symptom intact** — the next two diagnoses were made against evidence that had never changed. Every injection now asserts it landed; the habit was then extended to repair edits. **It has since caught itself once, which is the first evidence it works rather than merely sounding right.** The reviewer swept the surface by **neutralisation** rather than reading: no injection in this PR can pass against an unmodified file.

**An exclusion was deleted rather than narrowed a fourth time.** The `LiveState` guard needed to exempt its own source because the pattern contained its own search text. Three attempts produced three holes — a style rule that fell to an ordinary multi-line import, a specifier check with four routes around it, and a construction check the author tested *before* adopting and found evadable by `new ((await import('../server/state')).LiveState)({})`. Writing one character as a character class removes the self-match, so the file is scanned like every other. Verified match-for-match against the original: **29 matches, 0 disagreements.**

**The work is gated, and the headline stopped being a duration.** Four rounds were spent defending a millisecond range that **no test asserted** — `perf.test.ts` gates a cold build and an *in-process* refresh, a different quantity — and it was falsified by ordinary noise within a day (64 ms against a quoted floor of 65). `live.test.ts` now asserts `filesRead` < 1% of scanned, `filesSkipped` > 99%, `filesVerified` ≥ 99% of scanned, `verifyBytesRead ≤ scanned × 4 KB`, with `filesStale === 0` and the bucket partition **exact**. The mutation that matters: *hydrate without verifying* → `Expected > 2583.9 / Received 0`. **That regression is invisible on the clock and shows up nowhere else.**

**Staleness, stated at full strength.** Nine cases; **four are stale on `main` today** — persistence widens the window from ~1 s between SSE ticks to however long Mission Control is shut down, it does not create them. The 4 KB `boundaryHash` fixes **two of the four**, not four: the author's first draft had a rewrite landing at offset 0 that **passed**, and the honest repair was to split every rewrite into inside- and outside-window variants and assert the outside ones **stale**. *You cannot verify bytes you do not read* is now executable rather than a sentence. `logicFingerprint` over the parsers' source auto-invalidates on a parser edit, watched changing.

**Trust × cache: no leak, and the reason is structural.** A warm start serves an untrusted project's sessions — **and so does a cold build**. Trust gates *execution*; the index reads `~/.claude/projects`, the user's own history, and reading runs nothing. Independently confirmed by a second method: warm versus a cold build under the same trust state as oracle, five routes, identical.

**Review: PASS on both lenses, single anthropic model family** — not the ≥2-family panel the `security` lens requires at tier `full`, and after the brief withheld the author's reasoning it was still **not** `provenance`, because the reviewer carried its own prior parts. Stated, not papered over.

**Open, non-blocking, carried forward:** **N10** the `filesVerified` bracket is sound but its comment credits `machineGate()` when the real protection is the non-vacuity floor — at exactly 100 files, 99% tolerates **one** arrival, not ~26 · **N3** counted-vs-derived recorded not pinned, argument verified across eight corpus states · **C4** 24 h ceiling never fires in a long-running process · **C5** `logicFingerprint` separator comment overclaims (collision reproduced) · **C6** no `fsync`, safe because `payloadHash` rejects a torn write · **S5** cache `0644` beside a `0600` neighbour · **S7** "seven cases" where `CASES` has 9.

**And one finding that outlives this PR, filed as its own work.** `machineGate()` means the work gate **never executes in CI**. Measured: with the corpus pointed at an empty directory the suite reports **319 pass / 0 fail with 5 NOT VERIFIED** — *a headline identical to a full local run while five tests never ran*. Both the builder and the CEO cited "CI green on that SHA" as evidence for this work, six times between them. The cheap instrument is not stdout capture: `notVerified()` is one shared function and all five call sites route through it, so it can append to a marker file that `check.mjs` reads, reports and deletes — and in its strongest form **fails** when the count is non-zero on a machine that has a corpus.

`npm run check` exit 0 · **319 pass / 0 fail with 0 NOT VERIFIED** on the real 3.06 GB corpus. Classifier floor `lite`; tiered `full` by CEO judgement, which PR #49 would replace with a mechanism.
