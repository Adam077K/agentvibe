---
date: 2026-08-14
role: ceo
task: mc-stall-gate
tier: full
qa_verdict: PASS
---

#43: the stall pin flaked at **0.5%** margin (0.835 against a 0.750 bound, having passed at 0.746) because its gate watched **idle noise** while the operating confound was the **shared spawn floor** both paths pay. Fixing that surfaced a worse defect and the PR became a gate redesign: `resolutionMs` drew on `spread(asyncStalls)` — **the subject's own variance** — so a blocking implementation could widen the gate and be *withheld rather than caught*. A memoised lister (block on cache miss, async on hit, a shape already in `index-store.ts`) escaped **5 runs of 5**. What was being replaced failed **false-RED**; that fails **false-GREEN**, which for a guarantee is the worse one. `max − min` over five samples is not a dispersion estimate, so the term was removed entirely rather than re-based.
**Reviewed at Full tier, correctness lens, single model family — independence NOT met. 50 test executions.** Both mutations caught 5/5, plus one the author did not have (`changedFilesFor` → `execFileSync`, caught 5/5, so the pin binds both halves the issue names). The old-gate escape reproduced 4/4. **My stated concern was measured and refuted:** contention *improves* the margin — ratio max **0.151 loaded** (load avg 22 on 10 cores) against **0.229 unloaded** — because the control is twelve sequential blocking spawns absorbing load linearly while the async worst is one burst.
**The finding that outlives the PR (#43b): a timing gate cannot be made subject-independent.** Every syntactic path from subject to gate was removed and verified by trace. A stale-while-revalidate lister still went **invisible 4 of 4** by landing its synchronous revalidation inside the *control* window — and the leak **raised the pass line 96 → 143 ms**, the code under test widening its own bound by 48%. So the rule generalises: not only is any statistic computed from the subject's own timings under its influence, **so is any statistic taken from a window the subject may still be running in.** The test binds an implementation that BLOCKS, not one that LEAKS, and it now says so.
**Also corrected, all four found by the lens:** a paragraph asserting a resolution term that no longer exists, contradicted by the paragraph beneath it; the "nothing the subject does can move this threshold" claim, refuted causally; a diagnostic printing a **147.6 ms stall inside a 40.6 ms sweep** (max-over-rounds beside last-round survivors) on the exact regression the pin exists to catch; and a PR-body claim that nothing is skipped on fast machines, when the arithmetic says the withhold fires at `Fmax > 0.375 × Cmed` and the flaky runner sat at 0.61–0.84 — **there, the new gate abstains rather than asserts.** Struck rather than deleted, so the correction stays visible.
**Logged, not fixed:** the withhold branch is **unexercised** in 20 clean runs and every mutation, while `amax/fmax` hit 3.15 against the gate's own factor of 2 in 3 of 20 clean runs — only a 5.4–16.3 margin on `lineMs/fmax` keeps it quiet, and on a tighter runner those two facts meet. Its own PR, so this verdict is not stretched over code it did not cover. And #45 retires the whole instrument: interception has no window to contaminate. Author-declared verdict on a reviewer PASS — #24.
