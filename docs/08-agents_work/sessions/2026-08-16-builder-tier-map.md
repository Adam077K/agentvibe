---
date: 2026-08-16
role: builder
task: tier-map
tier: irreversible
qa_verdict: PASS
---

**Three path groups matched no rule and took `DEFAULT_TIER` (`lite`).** Measured before and after with `node scripts/classify.mjs`, never by reading the globs. `.mcp.json` — which starts a process for every session, today `npx -y @playwright/mcp@latest`, so the code that runs is not the code that was reviewed — was tiered at a CSS change. `.claude/workflows/**` held the binding gate's own source, tiered **below `scripts/lib/**`** one directory over. `.claude/sandbox/**` had no rule at all.
**Now:** `.mcp.json`, `.claude/workflows/qa.js`, `.claude/workflows/lib/gate-logic.mjs` and `.claude/sandbox/**` are `irreversible` + `enforcement: block`; the rest of `.claude/workflows/**` is `full` + `block`. Both gate files are floored because `gate-logic.mjs` is **not imported** by `qa.js` — the workflow runtime has no module import, so the arithmetic is mirrored inline by hand, and an edit to one is only correct if the other moves with it.
**The tree is deliberately not floored at irreversible.** `design.js` and `research.js` decide nothing a revert cannot undo; Founder sign-off on a prompt-wording edit is the ceremony that makes people route around the gate. Same carve-out the mission-control group draws, cited in the comment. Cost, counted: `.claude/workflows/README.md` and `design-screen.md` rise `trivial` → `full`, because no narrower rule can lower a tier.
**The `full` rule is justified by measurement, not fear:** from `abf26c2` to `c180cfe` all four `agent()` calls in `qa.js` omitted `agentType`, so the binary defaulted them to `general-purpose` and the binding judge held `Write` and `Edit` on the diff it was judging. Five days, not months.
**The sandbox rule departs from this file's own precedent** — the outbound-send and `trust.ts` notes both refuse patterns matching nothing, and `.claude/sandbox/` exists nowhere in this repo. Argued in the comment rather than left for review, with its failure mode named: if the open Founder decision puts the policy in `sandbox.network.deniedDomains` (a `settings.json` key, already `irreversible`) rather than a directory, the rule is dead coverage and must be deleted. **Flagged to the lead as the one item wanting a decision.**
**The existing deliberate-act guard did not fire, and that was the real gap.** `classifier.test.mjs` pins the irreversible set at three *within the mission-control tree only*, so four new irreversible rules landed silently. Added four tests covering the harness paths it never saw, mutation-checked against `origin/main`'s rule set — every assertion changes answer without the rules, so none passes vacuously.
**Verified by execution:** `node --test scripts/classifier.test.mjs` 28 pass / 0 fail (was 24). `npm run check` exit **0**, 466 pass / 0 fail across 18 steps including `check:mc` after `bun install --frozen-lockfile --cwd mission-control`. `ledger verify` 69 pass · **5 would_block** · 0 block — the expected clean-checkout number, no regression. The one lint warning (`.claude/agents/framer.md`) is pre-existing and untouched by this change.
**Not covered:** the tier map only. No `.mcp.json` credential policy, no sandbox implementation, and no change to `qa-lead-pass.yml` — this raises the number of paths that can demand `risk:irreversible`, which arms the F13 step whose known defect is recorded on the `trust.ts` entry.
